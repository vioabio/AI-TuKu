2026/6/22
1.代码格式化
使用create vue新建vue文件，版本为3.12.2
使用ESLINT和Prettier进行代码格式化

2.组件库
使用ant-design-vue：https://antdv.com/docs/vue/getting-started-cn

3.编写全局组件

4.编写导航栏组件

5.编写路由配置

6.引入第三方组件库axios，封装全局自定义请求
链接：https://axios.rest/pages/getting-started/first-steps
 
7.前端部署，自动根据swagger文档生成代码
链接：https://www.npmjs.com/package/@umijs/openapi

8.前端跨域部署
通过vite代理在前端解决跨域问题
前端代理工具Whistle:https://wproxy.org/docs/

9.全局状态管理
后端写一个单例
前端使用Pinia：https://pinia.vuejs.org/zh/getting-started.html

2026/6/23
完成前端初始化

2026/6/24
完成用户模块的前端部分

2026/6/25
完成用户模块的前端部分

1.编写用户管理页面（UserManagePage.vue）
- 搜索表单：支持按账号（userAccount）和用户名（userName）进行搜索
- 数据表格：展示用户 id、账号、用户名、头像（Image 组件）、简介、用户角色、创建时间、操作列
- 用户角色展示：管理员显示绿色 Tag，普通用户显示蓝色 Tag
- 创建时间格式化：使用 dayjs 格式化为 YYYY-MM-DD HH:mm:ss
- 分页功能：支持页码切换、每页条数切换，显示总条数
- 删除用户功能：点击删除按钮调用 deleteUserUsingPost 接口，删除后自动刷新列表
- 数据请求：通过 listUserVoByPageUsingPost 接口分页获取用户列表，页面加载时自动请求

2.全局权限控制（access.ts）
- 创建 src/access.ts 文件，在 main.ts 中引入
- 使用 Vue Router 的 beforeEach 导航守卫实现全局权限校验
- 首次加载时自动调用 fetchLoginUser 获取当前登录用户信息
- /admin 开头的路由仅允许 userRole 为 admin 的用户访问
- 无权限用户访问时提示”没有权限”，并重定向到登录页（携带 redirect 参数）
- 用户登录后可自动跳回之前访问的管理页面

3.完善全局导航栏（GlobalHeader.vue）
- 用户头像下拉菜单：登录后显示用户头像和用户名，点击展开下拉菜单
- “我的空间”菜单项：跳转到 /my_space 路由
- “退出登录”功能：调用 userLogoutUsingPost 接口，清除登录状态，跳转到登录页
- 菜单权限过滤：/admin 开头的菜单项仅对 admin 角色用户可见
  - 使用 computed 属性动态过滤菜单项
  - 非管理员用户看不到”用户管理”菜单

2026/6/27
完成用户传图模块的前端部分

1.创建图片审核常量文件（src/constants/picture.ts）
- 定义 PIC_REVIEW_STATUS_ENUM 审核状态枚举：REVIEWING(0)、PASS(1)、REJECT(2)
- 定义 PIC_REVIEW_STATUS_MAP 审核状态文案映射：0-待审核、1-通过、2-拒绝
- 定义 PIC_REVIEW_STATUS_OPTIONS 审核状态下拉表单选项数组

2.更新 API 类型定义（src/api/typings.d.ts）
- 新增 BaseResponseInt_ 类型（批量上传返回数量）
- 新增 PictureReviewRequest 类型（审核请求参数：id、reviewStatus、reviewMessage）
- 新增 PictureUploadByBatchRequest 类型（批量上传请求参数：searchText、count、namePrefix）
- 更新 PictureUploadRequest 类型（支持 fileUrl、picName、spaceId 字段）
- 更新 uploadPictureUsingPOSTParams 类型（同上）

3.新增 API 接口方法（src/api/pictureController.ts）
- doPictureReviewUsingPost：管理员图片审核接口（通过/拒绝）
- uploadPictureByUrlUsingPost：通过 URL 上传图片接口
- uploadPictureByBatchUsingPost：批量抓取图片接口

4.创建 URL 上传组件（src/components/UrlPictureUpload.vue）
- 使用 Ant Design Vue 的 input-group 复合输入框布局
- 输入图片 URL 地址，点击提交按钮上传
- 上传成功后通过 onSuccess 回调将图片信息传递给父组件
- 支持 loading 状态显示，上传中按钮不可点击
- 上传成功后预览已上传的图片

5.更新创建图片页面（src/pages/AddPicturePage.vue）
- 导入 UrlPictureUpload 组件
- 新增 uploadType 响应式变量，默认值为 'file'
- 使用 a-tabs 组件实现文件上传和 URL 上传两种方式的切换
- 文件上传 Tab：复用 PictureUpload 组件
- URL 上传 Tab：使用 UrlPictureUpload 组件（force-render 保持组件状态）

6.创建批量创建图片页面（src/pages/AddPictureBatchPage.vue）
- 搜索关键词输入框：支持输入必应图片搜索关键词
- 抓取数量输入框：使用 a-input-number 组件，限制 1-30 条
- 名称前缀输入框：自动补充序号，可选填
- 执行任务按钮：调用 uploadPictureByBatchUsingPost 接口
- 成功后显示创建条数并跳转到主页

7.更新路由配置（src/router/index.ts）
- 新增 /add_picture/batch 路由，指向 AddPictureBatchPage.vue
- 路由名称：”批量创建图片”

8.完善图片管理页面审核功能（src/pages/admin/PictureManagerPage.vue）
- 导入共享常量：PIC_REVIEW_STATUS_ENUM、PIC_REVIEW_STATUS_MAP、PIC_REVIEW_STATUS_OPTIONS
- 导入审核 API 方法：doPictureReviewUsingPost
- 移除组件内硬编码的审核常量和占位函数
- 实现 handleReview 函数：
  - 根据审核状态自动生成审核信息（通过/拒绝）
  - 调用 doPictureReviewUsingPost 接口提交审核
  - 审核成功后刷新列表数据
  - 审核失败时显示后端返回的错误信息
- 审核信息列支持展示：审核状态、审核信息、审核人、审核时间（dayjs 格式化）
- 操作列审核按钮条件渲染：未通过时显示”通过”按钮，未拒绝时显示”拒绝”按钮
- 审核状态下拉筛选功能：复用 PIC_REVIEW_STATUS_OPTIONS