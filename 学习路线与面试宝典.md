# AI-TuKu 智能云图库 — 从0到1学习路线与面试宝典

> 本文档面向"Vue + Java 全栈开发"岗位求职者，为 AI-TuKu 项目的完整学习指南。
> 涵盖技术栈全景、14 阶段学习路线、每个阶段的详细知识点、以及配套的面试题与参考答案。

---

# 第一部分：项目技术栈全景

## 1.1 技术栈一览

```
┌────────────────────────────────────────────────────────────────┐
│               AI-TuKu 智能云图库 技术架构                        │
├────────────────────────────────────────────────────────────────┤
│  部署层    │ Cloudflare Pages (前端) + Cloudflare Tunnel (后端)  │
├────────────────────────────────────────────────────────────────┤
│  前端层    │ Vue 3 + TypeScript 5.6 + Vite 6 + Ant Design Vue 4 │
│           │ Pinia 2 + Vue Router 4 + ECharts 6 + Axios         │
│           │ vue-cropper + file-saver + WebSocket               │
│           │ @umijs/openapi (API 代码生成)                       │
├────────────────────────────────────────────────────────────────┤
│  接口层    │ RESTful HTTP (40+ API) + WebSocket (wss://)        │
│           │ Knife4j / OpenAPI 2.0 文档自动生成                  │
├────────────────────────────────────────────────────────────────┤
│  后端层    │ Spring Boot 2.7.6 + Java 11 + Maven               │
│           │ 架构：DDD 四层（interfaces/application/domain/      │
│           │        infrastructure）+ shared 横切层              │
│           │ ORM：MyBatis-Plus 3.5 + MyBatis X                  │
│           │ 鉴权：Sa-Token 1.39（Kit 多账号体系 + RBAC）        │
│           │ 缓存：Caffeine（本地）+ Redis（分布式）两级缓存       │
│           │ 并发：LMAX Disruptor 3.4 环形缓冲区                 │
│           │ 文档：Knife4j 4.4 + OpenAPI 2.0                    │
│           │ 工具：Hutool 5.8 + Jsoup 1.15 + Lombok             │
├────────────────────────────────────────────────────────────────┤
│  中间件层  │ MySQL 8.0 + Redis + Caffeine + Disruptor           │
├────────────────────────────────────────────────────────────────┤
│  云服务层  │ 腾讯云 COS 5.6（对象存储）+ 万象CI（图片处理）      │
│           │ 阿里云百炼 DashScope（AI 图片扩图）                  │
├────────────────────────────────────────────────────────────────┤
│  设计模式  │ DDD + Repository + 模板方法 + 门面 + AOP           │
│           │ 生产者-消费者 + 观察者 + Read-Through + 依赖倒置     │
└────────────────────────────────────────────────────────────────┘
```

## 1.2 项目数据指标

| 指标 | 数据 |
|------|------|
| 开发周期 | 9 天（2026.6.20 ~ 6.28），全栈独立开发 |
| 后端文件 | ~134 个 Java 文件（DDD 四层架构） |
| 前端文件 | 14 个页面 + 10 个通用组件 |
| 数据库表 | 4 张业务表（user / picture / space / space_user） |
| API 接口 | 40+ RESTful 接口 + 6 个分析维度 |
| WebSocket | 50+ 并发连接无卡顿 |
| Bug 修复 | 45 个（涵盖编译错误/循环依赖/精度丢失等 10 类） |
| 设计模式 | 9 种设计模式实践 |

---

# 第二部分：14 阶段完整学习路线

```
阶段 1: 项目总览与架构设计      (0.5天)  → 理解业务全景与技术选型
阶段 2: 项目初始化与环境搭建    (1天)    → 前后端脚手架搭建
阶段 3: 用户模块开发            (1天)    → 注册/登录/Session鉴权
阶段 4: 图片模块开发            (1天)    → CRUD/文件上传/COS存储
阶段 5: 用户传图模块            (0.5天)  → 批量抓图/流程编排
阶段 6: 图片优化模块            (0.5天)  → 两级缓存/万象CI压缩
阶段 7: 空间模块开发            (0.5天)  → 多空间模式/RBAC权限
阶段 8: 图片功能扩展            (0.5天)  → 图片编辑/下载/裁剪
阶段 9: AI图片编辑模块          (1天)    → AI扩图/异步任务/轮询
阶段 10: 图库分析模块           (0.5天)  → 6维数据分析/ECharts
阶段 11: 团队空间模块           (1天)    → 成员管理/团队协作
阶段 12: 图片协同编辑           (1天)    → WebSocket/Disruptor
阶段 13: DDD架构重构            (1天)    → 四层架构/依赖倒置
阶段 14: 部署上线               (0.5天)  → Cloudflare部署/运维
```

---

# 第三部分：各阶段详细知识点 + 面试题 + 参考答案

---

## 阶段 1：项目总览与架构设计

### 学习目标
理解项目的业务全景、技术选型理由、架构设计思想。

### 详细知识点

#### 1.1 业务模型设计
- **三种空间模式**：公共图库（所有人可见）→ 私有空间（个人）→ 团队空间（多人协作）
- **用户角色体系**：普通用户（user）/ 管理员（admin）；空间角色：浏览者（viewer）/ 编辑者（editor）/ 管理员（admin）
- **核心业务流程**：注册 → 登录 → 创建空间 → 上传图片 → 浏览/搜索/编辑 → 团队协作

#### 1.2 技术选型决策
- **为什么选择 Spring Boot 而非 Spring MVC？** 自动配置、起步依赖、内嵌服务器，减少配置工作量
- **为什么选择 MyBatis-Plus 而非 JPA/Hibernate？** 更灵活的 SQL 控制、强大的条件构造器、分页插件、代码生成器
- **为什么选择 Vue 3 而非 React？** 更平缓的学习曲线、Composition API 更好的逻辑复用、更小的包体积
- **为什么选择 Ant Design Vue？** 企业级 UI 组件库、完善的中文文档、丰富的表单/表格/布局组件

#### 1.3 物理架构设计
- **开发环境**：前端 localhost:5173（Vite 代理到后端 8123）、后端 localhost:8123
- **生产环境**：前端 Cloudflare Pages（全球 CDN）、后端 Cloudflare Tunnel（内网穿透）

#### 1.4 三层架构 vs DDD 架构
- **传统三层**：Controller → Service → Mapper（业务复杂时 Service 膨胀）
- **DDD 四层**：interfaces → application → domain ← infrastructure（依赖倒置、领域内聚）

---

### 面试题 1-1：请用 3 分钟概述你的项目

**参考答案：**

AI-TuKu 是一个从零搭建的 AI 驱动的智能图片管理与团队协作平台。

**业务场景**：支持个人图库、团队共享图库、公共图库三种模式，用户可以上传/管理/搜索/编辑图片，团队空间内支持实时协同编辑，集成 AI 图片智能扩图能力，并提供 6 个维度的数据分析看板。

**技术选型**：
- 前端：Vue 3 + TypeScript + Vite + Ant Design Vue，通过 @umijs/openapi 自动生成类型安全的 API 调用层
- 后端：Spring Boot 2.7 + MyBatis-Plus，采用 DDD 四层架构（interfaces → application → domain ← infrastructure）
- 中间件：MySQL 8.0 + Redis + Caffeine 两级缓存 + LMAX Disruptor 异步队列
- 云服务：腾讯云 COS + 万象CI（图片存储与处理）、阿里云百炼 AI（智能扩图）
- 鉴权：Sa-Token Kit 模式实现双账号体系（Session + RBAC 空间权限）
- 实时通信：WebSocket + Disruptor 实现协同编辑（排除式广播）

**核心亮点**：
1. DDD 四层架构重构：从三层架构迁移到 DDD，5 个领域各自独立，依赖方向单向可控
2. RBAC 空间权限体系：JSON 配置化权限 + Sa-Token 多账号 + 自定义注解声明式鉴权
3. WebSocket + Disruptor 实时协同：编辑锁互斥 + 异步高吞吐 + 排除式广播
4. 模板方法模式重构图片上传：代码从 300 行降至父类 150 行 + 各子类 40 行
5. Snowflake ID 精度丢失系统性修复：后端序列化 + 前端保持 String + Spring 自动解析

**结果**：9 天独立完成全栈开发，40+ API 接口，上传成功率从 60% 提升至 98%，缓存命中率 70%。

---

### 面试题 1-2：如果重新从零开始，你会在哪些地方做出不同的技术决策？

**参考答案：**

**1. Spring Boot 3.x + Java 21 虚拟线程**
当前使用 Spring Boot 2.7.6 + Java 11。Spring Boot 2.7 已于 2023 年 11 月停止维护，3.x 版本支持：
- 虚拟线程（Virtual Threads）：WebSocket 和 HTTP 请求处理几乎零代码改动即可获得大幅并发提升
- GraalVM Native Image：编译为原生二进制，启动时间从秒级降至毫秒级
- 更好的可观测性集成（Micrometer Tracing）

**2. Docker 容器化**
当前后端裸机运行，应该从项目第一天就使用 Docker Compose 管理 MySQL + Redis + 后端，消除环境差异。

**3. 尽早引入测试**
当前项目零测试代码，应该在开发阶段就建立 JUnit 5 + Mockito 单元测试，核心 DomainService 覆盖率目标 80%+。

**4. WebSocket vs SSE 评估**
对于协同编辑的"服务端推送"场景，SSE 可能更轻量（基于 HTTP，无需额外协议升级），但 WebSocket 在双向通信上更有优势。需要根据实际需求权衡。

---

### 面试题 1-3：微服务架构和单体架构的取舍？你的项目适合拆成微服务吗？

**参考答案：**

**当前项目适合保持单体架构**，理由：
1. 业务规模：4 张表、5 个领域、日活预估低，单体架构完全够用
2. 团队规模：个人开发，微服务引入分布式复杂度得不偿失
3. 性能：单体架构无网络调用开销，DDD 分层已提供足够的内聚性

**如果要拆分微服务，演进路径**：
- 第一步：按业务域拆分（用户服务 / 图片服务 / 空间服务 / 分析服务）
- 第二步：引入 API Gateway（Spring Cloud Gateway）统一入口
- 第三步：引入 Nacos 做服务注册与配置中心
- 第四步：引入 Sentinel 做熔断限流
- 关键挑战：分布式事务（Seata）、跨服务鉴权（JWT/OAuth2）、数据一致性

**核心观点**：不要为了微服务而微服务，DDD 分层已经为未来拆分奠定了基础（领域边界清晰）。

---

## 阶段 2：项目初始化与环境搭建

### 学习目标
搭建前后端开发环境，初始化项目脚手架，配置数据库和中间件。

### 详细知识点

#### 2.1 后端初始化（Spring Boot）
- **Spring Initializr**：快速生成项目骨架
- **Maven 项目管理**：pom.xml 结构（dependencies / dependencyManagement / build）
- **配置文件分层**：`application.yml`（公共配置）→ `application-local.yml`（本地开发）→ `application-prod.yml`（生产）
- **Spring Boot 自动配置原理**：`@SpringBootApplication` → `@EnableAutoConfiguration` → `spring.factories`
- **Maven 镜像配置**：国内网络环境下配置阿里云 Maven 镜像（`~/.m2/settings.xml`）

#### 2.2 前端初始化（Vue 3 + Vite）
- **Vite 创建项目**：`npm create vite@latest`
- **项目目录结构**：`src/api/`（接口层）、`src/pages/`（页面）、`src/components/`（通用组件）、`src/router/`（路由）、`src/stores/`（状态管理）
- **Vite 配置**：开发代理 `server.proxy` 解决跨域问题
- **TypeScript 配置**：`tsconfig.json` 路径别名 `@` 映射到 `src/`

#### 2.3 数据库初始化
- **MySQL 8.0 安装与配置**
- **数据库设计**：字符集 `utf8mb4_unicode_ci`、存储引擎 InnoDB
- **DDL 编写**：建库 → 建表 → 索引设计

#### 2.4 MyBatis-Plus 配置
- **分页插件**：`PaginationInnerInterceptor(DbType.MYSQL)`
- **逻辑删除**：`@TableLogic` + `isDelete` 字段
- **自动填充**：`MetaObjectHandler` 自动填充 `createTime` / `updateTime`

---

### 面试题 2-1：Spring Boot 自动配置原理是什么？

**参考答案：**

Spring Boot 自动配置通过 `@EnableAutoConfiguration` 注解触发，核心流程：

1. **启动入口**：`@SpringBootApplication` 包含 `@EnableAutoConfiguration`
2. **加载配置类**：通过 `SpringFactoriesLoader` 读取 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`（2.7 版本用 `spring.factories`）
3. **条件装配**：每个自动配置类上有条件注解（如 `@ConditionalOnClass`、`@ConditionalOnMissingBean`、`@ConditionalOnProperty`），只在条件满足时加载
4. **示例**：`DataSourceAutoConfiguration` 在 classpath 中存在 `DataSource` 类且未手动定义 DataSource Bean 时自动配置数据源

**实际应用**：
- MyBatis-Plus 的 `MybatisPlusAutoConfiguration`：检测到 `SqlSessionFactory` 后自动配置
- 自定义配置：通过 `@Configuration` + `@ConditionalOnProperty` 实现条件化配置

**如何排除自动配置**：`@SpringBootApplication(exclude = {DataSourceAutoConfiguration.class})`

---

### 面试题 2-2：Vite 和 Webpack 有什么区别？为什么 Vue 3 项目推荐 Vite？

**参考答案：**

| 维度 | Vite | Webpack |
|------|------|---------|
| 开发服务器启动 | 冷启动 < 1s（ESM 按需编译） | 需打包整个应用，几十秒到分钟级 |
| 热更新（HMR） | 极快（只更新修改的模块） | 随项目增大变慢 |
| 构建 | 基于 Rollup 生产构建 | 自身完成打包 |
| 原理 | 利用浏览器原生 ES Module，请求时按需编译 | 先打包所有模块为 bundle |
| 插件生态 | 兼容 Rollup 插件，生态快速增长 | 插件生态成熟丰富 |

**为什么快**：
1. Vite 将模块分为"依赖"和"源码"：依赖用 esbuild（Go 编写，快 10-100 倍）预构建；源码按需编译
2. 开发时不打包，浏览器通过 ESM import 直接请求，Vite 拦截请求实时编译返回

**本项目中的应用**：Vite 配置 `server.proxy` 将 `/api` 请求代理到后端 8123 端口解决跨域。

---

### 面试题 2-3：MyBatis-Plus 的分页插件是如何工作的？

**参考答案：**

MyBatis-Plus 分页插件基于 MyBatis 的拦截器（Interceptor）机制实现：

1. **注册分页插件**：
```java
@Bean
public MybatisPlusInterceptor mybatisPlusInterceptor() {
    MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
    interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.MYSQL));
    return interceptor;
}
```

2. **原理**：
   - MyBatis 在执行 SQL 前会调用已注册的拦截器
   - `PaginationInnerInterceptor` 拦截 `Executor.query()` 方法
   - 检测到 `IPage` 参数时，自动拼接 `LIMIT offset, size`（MySQL 方言）
   - 同时执行 `COUNT` 查询获取总记录数

3. **方言适配**：不同的 `DbType` 生成不同方言的分页 SQL（MySQL → LIMIT、Oracle → ROWNUM、PostgreSQL → OFFSET FETCH）

4. **本项目遇到的坑**：未配置分页插件导致 `IPage` 参数不生效，返回全部数据而非分页数据。根本原因是缺少分页拦截器拼接 LIMIT 子句。

---

## 阶段 3：用户模块开发

### 学习目标
实现用户注册/登录/登出、Session-Cookie 鉴权、密码加密、全局异常处理。

### 详细知识点

#### 3.1 用户注册
- **密码加密**：BCrypt（盐值加密），`BCryptPasswordEncoder.encode()`
- **参数校验**：`@NotBlank`、`@Size`、`@Pattern` 等 JSR-303 注解
- **账号唯一性**：数据库 UNIQUE 索引 + 业务层二次校验
- **DTO/Entity/VO 分层**：
  - DTO（Data Transfer Object）：接收前端请求参数
  - Entity：数据库实体（与表映射）
  - VO（View Object）：返回给前端的视图对象（脱敏，不含密码）

#### 3.2 用户登录与 Session 鉴权
- **Session-Cookie 机制**：
  - 登录成功 → 服务端 `HttpSession.setAttribute("user", userVO)`
  - 后续请求 → 浏览器自动携带 Cookie（JSESSIONID）→ 服务端 `session.getAttribute()` 取用户信息
- **Session 存储**：默认内存存储 → 可扩展为 Redis 存储（Spring Session）

#### 3.3 AOP 切面鉴权
- **自定义注解** `@AuthCheck(mustRole = "admin")`
- **切面实现** `AuthInterceptor`：
  - `@Around("@annotation(authCheck)")`
  - 从 Session 获取当前用户 → 比较角色 → 不匹配抛异常

#### 3.4 全局异常处理
- **`@RestControllerAdvice` + `@ExceptionHandler`**
- **统一响应格式**：`BaseResponse<T>`（code + data + message）
- **异常分层**：
  - `BusinessException`（业务异常，code 4xxxx）
  - `RuntimeException`（系统异常，code 50000）

#### 3.5 实体字段不匹配问题
- **问题**：User 实体定义了 `vipExpireTime` 等字段，但数据库表中无此列
- **根因**：MyBatis-Plus 默认所有字段映射到数据库列
- **解决**：`@TableField(exist = false)` 标注非数据库字段

---

### 面试题 3-1：Session-Cookie 和 JWT 的区别？什么场景下选哪个？

**参考答案：**

| 维度 | Session-Cookie | JWT |
|------|---------------|-----|
| 状态存储 | 服务端存储（有状态） | 客户端存储（无状态） |
| 扩展性 | 难以水平扩展（需 Session 共享） | 天然支持分布式 |
| 安全性 | Cookie 可设置 HttpOnly/Secure | Token 存储在客户端，存在 XSS 风险 |
| 主动失效 | 服务端删除 Session 即可 | 无法主动失效（需配合黑名单） |
| 携带方式 | Cookie 自动携带 | Header Authorization 手动携带 |
| 适合场景 | 传统 Web 应用、单体架构 | 分布式/微服务、移动端、跨域 API |

**本项目选型**：使用 Session-Cookie 是因为项目为传统 Web 前后端分离架构，Session 简单可靠，且单体部署无水平扩展需求。同时使用 Sa-Token 的 Kit 模式，为空间权限体系增加独立的 Token 维度的鉴权。

**JWT 的问题**：
1. Token 一旦签发，在过期前无法主动撤销（用户修改密码后旧 Token 仍有效）
2. Payload 是 Base64 编码而非加密，不能存放敏感信息
3. Token 体积较大，每次请求都携带

---

### 面试题 3-2：Spring AOP 的原理是什么？JDK 动态代理和 CGLIB 有什么区别？

**参考答案：**

**AOP 核心概念**：
- Aspect（切面）：横切关注点的模块化（如鉴权、日志、事务）
- Join Point（连接点）：方法执行点
- Pointcut（切入点）：匹配连接点的表达式
- Advice（通知）：在切入点执行的动作（@Before / @After / @Around）

**Spring AOP 实现原理**：
1. Spring AOP 基于动态代理实现（非编译期织入，区别于 AspectJ）
2. 当 Bean 有接口时 → JDK 动态代理（`Proxy.newProxyInstance`）
3. 当 Bean 无接口时 → CGLIB 代理（字节码生成子类）
4. Spring Boot 2.x 默认使用 CGLIB（`spring.aop.proxy-target-class=true`）

**JDK 动态代理 vs CGLIB**：

| 维度 | JDK 动态代理 | CGLIB 代理 |
|------|------------|-----------|
| 原理 | 反射，基于接口生成代理 | ASM 字节码，基于继承生成子类 |
| 限制 | 目标类必须实现接口 | 不能代理 final 类/方法 |
| 性能 | 创建快，调用略慢（反射） | 创建慢（字节码生成），调用快 |
| 本项目 | AuthInterceptor 使用 @Around 切面鉴权 |

**本项目中的应用**：`AuthInterceptor` 使用 `@Around("@annotation(com.vio.aitukuviobe.infrastructure.annotation.AuthCheck)")` 拦截带有 `@AuthCheck` 注解的方法，从 Session 获取用户信息并校验角色。

---

### 面试题 3-3：BCrypt 加密为什么安全？彩虹表攻击是什么？

**参考答案：**

**BCrypt 安全特性**：
1. **内置盐值（Salt）**：每次加密自动生成随机盐值，同一密码两次加密结果不同
2. **可配置的计算成本**：`BCryptPasswordEncoder(strength=10)`，strength 越高哈希计算越慢（指数关系，2^strength 次迭代）
3. **抗暴力破解**：单次验证耗时 ~100ms，攻击者无法高速尝试

**彩虹表攻击**：
- 彩虹表是预先计算好的"明文 → 哈希"映射表
- 如果密码仅做一次简单哈希（如 MD5、SHA-256），攻击者可通过查表反查明文
- **BCrypt 为什么能防**：每个密码的盐值不同，攻击者无法预先为所有盐值生成彩虹表

**本项目实战**：
```java
// 注册时加密
String encryptedPassword = passwordEncoder.encode(userPassword);
// 登录时验证
passwordEncoder.matches(inputPassword, storedEncryptedPassword);
```

---

## 阶段 4：图片模块开发

### 学习目标
实现图片 CRUD、文件上传到腾讯云 COS、万象 CI 图片处理。

### 详细知识点

#### 4.1 图片 CRUD
- **RESTful API 设计**：`POST /picture/upload`、`GET /picture/{id}`、`POST /picture/list/page/vo`
- **MyBatis-Plus 条件构造器**：`LambdaQueryWrapper` + 动态查询 + 排序
- **MyBatis-Plus 分页**：`Page<Picture> page = new Page<>(current, pageSize)`

#### 4.2 腾讯云 COS 对象存储
- **COS Java SDK**：`cos_api:5.6.227`（依赖正确的包，非 `tencentcloud-sdk-java`）
- **核心操作**：`putObject`（上传）、`deleteObject`（删除）、`getObjectUrl`（获取访问 URL）
- **ACL 权限**：`CannedAccessControlList.PublicRead`（公共读）、`Private`（私有）
- **配置管理**：SecretId/SecretKey 通过 `application-local.yml` 管理（不提交 Git）

#### 4.3 万象 CI 图片处理
- **`PicOperations`**：上传时附带图片处理规则
- **处理规则**：规则 1 — WebP 格式压缩（质量 80%）；规则 2 — 缩略图（256×256，最小边适应）
- **`ProcessResults`**：万象 CI 处理完成后返回处理结果的 Key 信息
- **CAM 角色**：需要配置 `CI_QCSrole` 服务角色授权

#### 4.4 CI 降级容错设计
- **问题**：万象 CI 不可用时（未配置 CAM 角色）上传直接失败
- **方案**：捕获 `CosServiceException` → 判断是否角色相关错误 → 回退为基础 `putObject`
- **关键**：降级无损（图片正常上传），只是缺少压缩和缩略图能力

---

### 面试题 4-1：对象存储（COS/OSS/S3）中，公共读和私有桶的 URL 访问方式有什么区别？

**参考答案：**

**公共读（PublicRead）**：
- 对象 URL 可以直接在浏览器访问
- 优点：简单，前端直接用 URL 渲染图片
- 缺点：任何人知道 URL 都能访问，不适合敏感内容
- 本项目使用场景：公共图库的图片展示

**私有桶（Private）**：
- 对象 URL 直接访问返回 403
- 需要通过 SDK 生成预签名 URL（Pre-signed URL），携带签名参数
- 预签名 URL 有有效期（如 30 分钟），过期后不可访问
- 优点：安全可控，防爬虫/盗链
- 本项目的问题：初期未设置 ACL 导致 403 → 添加 `setCannedAcl(PublicRead)` 解决

**实际选型建议**：
- 用户头像/公开图片 → 公共读（配合 CDN）
- 身份证/合同等敏感文件 → 私有桶 + 预签名 URL + 防盗链

---

### 面试题 4-2：文件上传功能需要做哪些安全校验？

**参考答案：**

**前端层面**：
1. 文件类型校验：`accept="image/*"` 限制选择器只能选图片
2. 文件大小校验：组件内 `beforeUpload` 检查 `file.size`

**后端层面（必须有，前端可被绕过）**：
1. **文件大小二次校验**：`FileManager` 中校验 `file.length() > maxFileSize`
2. **文件类型魔数校验**：读取文件头魔数（Magic Bytes）判断真实类型，而非信任扩展名或 Content-Type
   - JPEG：`FF D8 FF`
   - PNG：`89 50 4E 47`
   - GIF：`47 49 46 38`
3. **文件后缀白名单**：只允许 `jpg/jpeg/png/webp/gif`
4. **文件名处理**：使用 UUID 重命名，防止路径穿越攻击（`../../../etc/passwd`）
5. **内容安全审核**：接入 AI 审核服务检测违规内容（NSFW）
6. **存储安全**：上传文件不可直接执行（COS 设置为静态托管或私有桶）

**本项目现状**：已实现大小限制（20MB），魔数校验和内容审核为后续优化方向。

---

### 面试题 4-3：你在项目中遇到的 COS URL 双斜杠和缩略图路径丢失问题，根因是什么？如何排查的？

**参考答案：**

**根因分析**：
1. **双斜杠**：`FileManager` 拼接 URL 逻辑为 `host + "/" + uploadPath`，但 `uploadPath` 已以 `/` 开头 → `https://bucket.cos.region.myqcloud.com//images/xxx.webp`
2. **缩略图路径丢失**：`CosManager` 中处理万象 CI 返回结果时，缩略图 key 仅取文件名部分（如 `thumbnail.jpg`），丢失了目录路径（应为 `images/2024/06/thumbnail.jpg`）

**排查过程**：
1. 前端图片列表不显示 → 打开浏览器 F12 → 查看 Network 中图片请求 URL → 发现双斜杠
2. 修复后仍然不显示 → 浏览器直接访问图片 URL → 403 Forbidden → 添加 PublicRead ACL
3. 修复后部分图片不显示 → 对比正常和异常的 URL → 发现缩略图路径缺少目录前缀

**经验教训**：
1. URL 拼接应使用成熟的工具类（如 `UriComponentsBuilder`）而非手动字符串拼接
2. 对象存储路径应在常量类中统一定义规范
3. 万象 CI 返回的 `ProcessResults` 中的 Key 是完整路径，应优先使用而非自己拼接

---

## 阶段 5：用户传图模块

### 学习目标
实现批量图片抓取、HTML 解析、URL 图片上传。

### 详细知识点

#### 5.1 Jsoup HTML 解析
- **核心 API**：`Jsoup.connect(url).get()` → `doc.select("css选择器")`
- **爬虫伦理**：遵守 robots.txt、控制请求频率、设置 User-Agent
- **本项目使用**：解析必应图片搜索结果 `cn.bing.com/images/async`

#### 5.2 批量上传流程编排
- **流程**：搜索关键词 → HTML 抓取 → URL 提取 → URL 转义 → 逐张下载 → URL 上传 → 入库
- **容错策略**：单张失败不影响整体（`try-catch` 逐张处理）

#### 5.3 Maven 依赖管理
- **阿里云 Maven 镜像**：`~/.m2/settings.xml` 配置 `<mirror>`
- **依赖缓存**：本地 `~/.m2/repository` 缓存已下载的 jar

---

### 面试题 5-1：Jsoup 解析 HTML 的原理是什么？它是如何构建 DOM 树的？

**参考答案：**

Jsoup 是一个 Java 的 HTML 解析器，核心流程：
1. **HTTP 请求**：`Jsoup.connect(url).get()` 发起 HTTP GET 请求获取 HTML
2. **词法分析**：将 HTML 字符串解析为 Token 序列（标签、属性、文本、注释等）
3. **DOM 树构建**：根据 Token 序列构建 DOM 树（类似浏览器的解析过程）
4. **CSS 选择器查询**：`doc.select(".mimg")` 遍历 DOM 树匹配元素
5. **容错性**：Jsoup 能处理不规范的 HTML（自动补全未闭合标签、嵌套错误等）

**本项目实战**：
```java
Document doc = Jsoup.connect("https://cn.bing.com/images/async?q=风景")
    .userAgent("Mozilla/5.0")
    .get();
Elements imgs = doc.select("img.mimg");
for (Element img : imgs) {
    String url = img.attr("src");
    // 下载并上传到 COS
}
```

---

## 阶段 6：图片优化模块

### 学习目标
实现 Caffeine + Redis 两级缓存架构、缓存穿透/雪崩防护。

### 详细知识点

#### 6.1 Caffeine 本地缓存
- **核心特性**：W-TinyLFU 淘汰算法（Window TinyLFU）、近乎最优命中率
- **配置项**：
  - `initialCapacity(1024)`：初始容量
  - `maximumSize(10000)`：最大条目数
  - `expireAfterWrite(5, TimeUnit.MINUTES)`：写入后过期
- **使用方式**：`cache.get(key, k -> loadFromDB())`（Read-Through）

#### 6.2 Redis 分布式缓存
- **Spring Data Redis**：`RedisTemplate<String, Object>`
- **序列化**：JSON 序列化（Jackson）存储对象
- **过期时间随机化**：`TTL ± 随机偏移` 防雪崩

#### 6.3 缓存 Key 设计
- **方案**：查询条件 JSON → MD5 → `yupicture:listPictureVOByPage:{hash}`
- **优点**：固定长度、减少 Redis 内存占用

#### 6.4 缓存三大问题
- **缓存穿透**：查询不存在的数据 → 布隆过滤器 或 空值缓存短时间
- **缓存击穿**：热点 Key 过期 → 互斥锁加载 或 永不过期
- **缓存雪崩**：大量 Key 同时过期 → 随机过期时间 + 多级缓存

#### 6.5 Read-Through 缓存模式
- **流程**：请求命中 Caffeine → 未命中查 Redis → 未命中查 DB → 回填 Caffeine + Redis

---

### 面试题 6-1：请详细解释缓存穿透、缓存击穿、缓存雪崩的区别和各自的解决方案。

**参考答案：**

**缓存穿透**（查询不存在的数据）：
- 场景：恶意查询不存在的 ID（如 id=-1），每次都穿透缓存查 DB
- 解决：
  1. **布隆过滤器**：前置过滤不存在的 Key（有误判率，但不会漏判）
  2. **空值缓存**：将 null 结果也缓存（短过期时间，如 60 秒）
  3. **本项目做法**：空结果不写入缓存，依赖前端参数校验拦截非法 ID

**缓存击穿**（热点 Key 过期）：
- 场景：秒杀商品的缓存刚好过期，大量请求直接打到 DB
- 解决：
  1. **互斥锁**：只有获得锁的线程去加载 DB，其他等待
  2. **逻辑过期**：热点 Key 设置永不过期，后台异步刷新
  3. **本项目**：Caffeine 作为本地缓存先扛一波，Redis 过期后 Caffeine 可能还有数据

**缓存雪崩**（大量 Key 同时过期）：
- 场景：一批缓存设置了相同的过期时间，同时过期导致 DB 压力激增
- 解决：
  1. **随机过期时间**：TTL 增加随机偏移（本项目：5-10 分钟随机）
  2. **多级缓存**：Caffeine（本地）→ Redis（分布式）→ DB
  3. **限流降级**：保护 DB，部分请求快速失败

**关键区分口诀**：
- 穿透：查不存在的数据 → 布隆过滤器
- 击穿：一个热点挂了 → 互斥锁
- 雪崩：大片缓存同时挂 → 随机过期

---

### 面试题 6-2：Caffeine 的 W-TinyLFU 算法相比 LRU 有什么优势？

**参考答案：**

**LRU（Least Recently Used）的缺陷**：
- 只考虑"最近访问"，不考虑"访问频率"
- 容易被"扫描性"访问污染：如果大量一次性数据被访问，会把高频热点数据挤出去
- 例如：批量导出报表操作扫描大量数据 → 热点缓存被冲掉

**W-TinyLFU 的设计**：
1. **Window Cache**：新进入的数据先放入 Window 区（LRU），防止新数据被低频数据立即淘汰
2. **Main Cache**（SLRU）：分 Probation（考察区）和 Protected（保护区）
3. **TinyLFU**：使用 Count-Min Sketch 维护访问频率计数器，判断是否淘汰
4. **核心思想**：淘汰时综合考虑"最近性"和"频率"，保护高频数据

**优势**：在大多数场景下命中率接近最优（Optimal），且内存开销可控。

**本项目使用**：图片列表查询的缓存，高频率首页查询不会被一次性扫描操作冲掉。

---

### 面试题 6-3：数据更新时如何保证缓存一致性？

**参考答案：**

**Cache Aside 模式（旁路缓存）**：
```
写操作：更新 DB → 删除缓存（或更新缓存）
读操作：查缓存 → 命中返回 / 未命中查 DB → 回填缓存
```

**为什么是"删缓存"而非"更新缓存"**：
- 如果更新缓存，并发写可能造成缓存和 DB 数据不一致
- 删除是幂等操作，更安全

**为什么先更新 DB 再删缓存**：
- 如果先删缓存 → 更新 DB 前另一个线程读 → 读到旧数据 → 写入缓存 → 缓存是脏数据
- 如果先更新 DB → 删缓存 → 其他线程读到新数据或缓存为空 → 回填时读新数据

**本项目的不足与改进**：
- 当前只在数据更新后删 Redis 缓存，但 Caffeine 本地缓存未同步失效
- 改进方案：Redis Pub/Sub 广播缓存失效消息 → 所有实例监听 → 清除本地 Caffeine
- 进一步：考虑延迟双删（删缓存 → 更新 DB → 等一会 → 再删缓存）

**最终一致性认知**：对于图片列表这种场景，允许短时间的不一致（Caffeine 5 分钟过期），不需要强一致性。

---

## 阶段 7：空间模块开发

### 学习目标
实现三种空间模式、Sa-Token RBAC 权限体系、JSON 配置化权限。

### 详细知识点

#### 7.1 空间模式设计
- **私有空间**：`spaceType = 0`，只有创建者可见
- **团队空间**：`spaceType = 1`，成员按角色（viewer/editor/admin）拥有不同权限
- **空间等级**：普通版 / 专业版 / 旗舰版，控制最大上传数和容量

#### 7.2 Sa-Token 权限框架
- **核心能力**：登录认证、权限校验、Token 管理、Session 管理
- **Kit 模式**：`StpKit` 创建独立的 `StpLogic` 实例，实现多账号体系
  ```java
  public class StpKit {
      public static final StpLogic SPACE = new StpLogic("space");
  }
  ```
- **与原有 Session 共存**：用户登录用 Session → 空间鉴权用 Sa-Token

#### 7.3 RBAC 权限配置化
- **权限配置文件**：`spaceUserAuthConfig.json` 定义权限项 × 角色映射
- **5 个权限项**：
  - `spaceUser:manage`：管理空间成员
  - `picture:view`：查看图片
  - `picture:upload`：上传图片
  - `picture:edit`：编辑图片
  - `picture:delete`：删除图片
- **3 种角色映射**：
  - viewer → [picture:view]
  - editor → [picture:view, upload, edit]
  - admin → 全部 5 个权限

#### 7.4 自定义权限注解
- `@SaSpaceCheckPermission`：合并 `@SaCheckPermission(type="space")`
- **多级降级判断**：从请求 URL 推断上下文 → public / private / team → 按规则返回权限列表

#### 7.5 RequestWrapper 解决 Body 重复读取
- **问题**：Servlet InputStream 只能读一次，Sa-Token 读完后 Controller 无法再读
- **解决**：编写 `RequestWrapper`（缓存 Body 字节数组）+ `HttpRequestWrapperFilter` 过滤器

---

### 面试题 7-1：RBAC 权限模型是什么？你用过的项目中是怎么落地的？

**参考答案：**

**RBAC（Role-Based Access Control）** 核心三要素：
- **User（用户）**：操作主体
- **Role（角色）**：权限的集合（viewer / editor / admin）
- **Permission（权限）**：最小操作单元（picture:upload / picture:delete）

**RBAC 三层次模型**：
- **RBAC0**：User → Role → Permission（基础模型）
- **RBAC1**：角色继承（admin 角色自动拥有 editor 的所有权限）
- **RBAC2**：职责分离（一个用户不能同时拥有"下单"和"审批订单"角色）
- **RBAC3**：RBAC1 + RBAC2 合并

**本项目的落地**：
1. 系统中存在两层角色：
   - **系统角色**：user（普通用户）、admin（管理员），通过 `userRole` 字段区分
   - **空间角色**：viewer（浏览者）、editor（编辑者）、admin（管理员），通过 `space_user` 表管理
2. 权限配置外置化为 JSON 文件，修改权限规则无需改代码
3. Sa-Token Kit 模式实现用户认证和空间授权两套独立的鉴权体系
4. `StpInterfaceImpl.getPermissionList()` 动态根据请求上下文返回当前用户的权限集合
5. Controller 层通过 `@SaSpaceCheckPermission` 注解声明式鉴权

**相比硬编码的优势**：
- 权限规则修改只改 JSON，无需重新部署
- 新增角色只需添加 JSON 配置项
- Controller 代码中零硬编码的 if-else 权限判断

---

### 面试题 7-2：Sa-Token 和 Spring Security 有什么区别？为什么选择 Sa-Token？

**参考答案：**

| 维度 | Sa-Token | Spring Security |
|------|---------|----------------|
| 学习成本 | 低（注解即用，中文文档） | 高（过滤器链、配置复杂） |
| 功能完整性 | 登录/鉴权/踢人/多账号 | 功能全面但需自定义扩展 |
| 集成复杂度 | 引入即用，API 简洁 | 需理解 Security 架构 |
| 多账号体系 | Kit 模式天然支持 | 需自定义 Provider |
| 性能 | 轻量（无过滤器链） | 过滤器链有开销 |
| 社区生态 | 国内活跃 | 全球标准 |

**本项目选择 Sa-Token 的原因**：
1. 快速开发需求（9 天项目），Sa-Token 集成成本低
2. Kit 模式天然支持多账号体系（Session + Space Token 独立管理）
3. Redis 集成开箱即用，分布式场景方便扩展
4. 中文文档对新手友好

**Spring Security 更适合的场景**：企业级大型项目、需要 OAuth2/LDAP/SAML 等标准协议集成、安全认证要求严格的场景。

---

### 面试题 7-3：你是如何解决 Servlet Body 只能读取一次的问题的？

**参考答案：**

**问题背景**：
- Servlet 的 `HttpServletRequest.getInputStream()` 返回的流只能消费一次
- Sa-Token 在执行权限校验时需要读取请求体（获取 pictureId 等参数）
- Controller 方法再次读取请求体时，流已为空 → 参数绑定失败

**解决方案：自定义 RequestWrapper**

```java
public class RequestWrapper extends HttpServletRequestWrapper {
    private final byte[] body;  // 缓存请求体
    
    public RequestWrapper(HttpServletRequest request) {
        super(request);
        // 读取原始流并缓存
        this.body = StreamUtils.copyToByteArray(request.getInputStream());
    }
    
    @Override
    public ServletInputStream getInputStream() {
        // 每次调用返回新的流（基于缓存的 byte[]）
        return new ByteArrayServletInputStream(body);
    }
}
```

**配合过滤器使用**：
```java
@WebFilter(urlPatterns = "/*")
public class HttpRequestWrapperFilter implements Filter {
    @Override
    public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain) {
        if (request instanceof HttpServletRequest) {
            // 包装原始请求，后续所有读取都经过 RequestWrapper
            chain.doFilter(new RequestWrapper((HttpServletRequest) request), response);
        }
    }
}
```

**核心思想**：在过滤器中一次性读完 Body 并缓存在 byte 数组中，后续每次 `getInputStream()` 都基于缓存的 byte[] 重新创建流。

---

## 阶段 8：图片功能扩展

### 学习目标
实现图片编辑、下载、裁剪功能。

### 详细知识点

#### 8.1 图片编辑
- **编辑操作**：name / introduction / category / tags 更新
- **权限校验**：仅图主和管理员可编辑
- **乐观锁 vs 悲观锁**：editTime 字段实现简易乐观锁

#### 8.2 图片下载
- **前端**：`file-saver` 库实现浏览器下载
- **直接 URL 下载**：COS 公网 URL → canvas 转 Blob → `saveAs(blob, filename)`

#### 8.3 图片裁剪
- **vue-cropper**：支持拖拽裁剪框、固定比例、旋转
- **裁剪流程**：选择图片 → 调整裁剪区域 → 确认 → 获取裁剪后的 Blob → 上传到 COS → 更新 Picture 记录

---

### 面试题 8-1：乐观锁和悲观锁的区别？在你的项目中有什么应用？

**参考答案：**

| 维度 | 乐观锁 | 悲观锁 |
|------|--------|--------|
| 思想 | 假设冲突少，提交时检查 | 假设冲突多，先加锁再操作 |
| 实现 | 版本号 / 时间戳 CAS | SELECT FOR UPDATE / synchronized |
| 性能 | 无锁等待，并发高 | 有锁等待，并发低 |
| 适用场景 | 读多写少，冲突概率低 | 写多，冲突概率高 |
| 失败处理 | 重试或提示用户 | 阻塞等待 |

**本项目中的应用**：
- **图片编辑**：更新时比较 `editTime`，`UPDATE picture SET ... WHERE id=? AND editTime=?`，如果 editTime 已被其他请求修改，则更新失败
- **编辑锁**：协同编辑中使用 `ConcurrentHashMap` 实现编辑锁（悲观锁），同一时间只有一人编辑

---

## 阶段 9：AI 图片编辑模块

### 学习目标
对接阿里云百炼 AI、异步任务模式、前端轮询设计。

### 详细知识点

#### 9.1 阿里云百炼 DashScope API
- **API 模式**：RESTful HTTP + Bearer Token 鉴权
- **扩图接口**：`aigc/image2image/out-painting`
- **异步模式**：`X-DashScope-Async: enable` → 返回 `taskId` → 轮询结果

#### 9.2 异步任务模型
- **任务生命周期**：PENDING（等待处理） → RUNNING（处理中） → SUCCEEDED（成功）/ FAILED（失败）
- **轮询策略**：每 3 秒查询一次 `GET /api/ai/out-painting/result?taskId=xxx`
- **超时处理**：30 秒未完成提示用户重试

#### 9.3 前端轮询实现
- **setInterval 轮询**：`setInterval(pollTask, 3000)`
- **组件卸载清理**：`onUnmounted(() => clearInterval(timer))` 防止内存泄漏
- **状态管理**：PENDING → 显示加载动画；SUCCEEDED → 渲染结果图 + 一键应用按钮；FAILED → 显示错误信息

#### 9.4 错误诊断设计
- **请求日志**：打印完整请求体（JSON）
- **响应日志**：打印 HTTP 状态码和响应体
- **失败日志**：`log.error` 打印 taskId + output.code + output.message

---

### 面试题 9-1：异步任务模式 vs 同步模式在 AI API 调用中各有什么优劣？

**参考答案：**

**同步模式**：
- 流程：客户端请求 → 服务端调用 AI API → 等待处理完成（10-60s） → 返回结果
- 优点：逻辑简单、一次请求完成
- 缺点：
  - HTTP 连接长时间占用（可能超时）
  - 用户无进度反馈，体验差
  - 服务端线程阻塞等待

**异步模式**：
- 流程：客户端请求 → 服务端提交任务，返回 taskId → 服务端立即响应 → 客户端轮询查询结果
- 优点：
  - 服务端线程立即释放，不阻塞
  - 用户有进度反馈（轮询间隔显示状态）
  - 可支持更大并发
- 缺点：
  - 实现复杂（需要状态管理）
  - 轮询有延迟
  - 需要持久化 taskId

**本项目的选型**：AI 扩图处理需要 10-12 秒，同步模式会导致 HTTP 超时，因此选择异步模式。

**其他方案对比**：
- **WebSocket 推送**：任务完成时服务端主动推送（延迟最低，但增加连接管理复杂度）
- **SSE（Server-Sent Events）**：基于 HTTP 的单向推送（比 WebSocket 简单，但不能双向通信）
- **Webhook**：任务完成时回调指定 URL（解耦但需要公网可达的回调地址）

---

### 面试题 9-2：如何设计一个可复用的异步任务框架？

**参考答案：**

**核心抽象**：
```java
// 任务接口
interface AsyncTask<T, R> {
    String submit(T input);     // 提交任务，返回 taskId
    TaskStatus getStatus(String taskId);  // 查询状态
    R getResult(String taskId); // 获取结果
}

// 任务状态
enum TaskStatus {
    PENDING, RUNNING, SUCCEEDED, FAILED, TIMEOUT
}
```

**关键组件**：
1. **任务存储**：Redis Hash（taskId → {status, result, createTime}）
2. **任务调度器**：线程池 + `@Async` 执行具体 AI API 调用
3. **超时管理**：定时任务扫描超时任务（>30s PENDING → TIMEOUT）
4. **结果通知**：任务完成后通过 WebSocket / SSE 推送（优化轮询体验）

**本项目可复用**：当前的异步扩图流程提取为通用框架后，新增 AI 功能只需实现具体的 `submit` 和 `getResult` 逻辑。

---

## 阶段 10：图库分析模块

### 学习目标
实现 6 维数据分析（空间使用量/大小/分类/标签/用户/排行）、ECharts 图表展示。

### 详细知识点

#### 10.1 数据分析维度
- **空间使用量分析**：按时间段统计上传量（折线图）
- **空间大小分析**：每个用户上传的总大小（饼图/柱状图）
- **分类分析**：按 picture.category 统计各分类数量（饼图）
- **标签分析**：按 picture.tags 统计各标签出现频率（词云/柱状图）
- **用户分析**：空间成员的活跃度统计
- **排行分析**：上传数量排行榜

#### 10.2 SQL 聚合查询
- **GROUP BY + COUNT/SUM**：分组统计
- **时间函数**：`DATE_FORMAT(createTime, '%Y-%m-%d')` 按天聚合
- **JSON 字段处理**：MySQL JSON 函数处理 tags 字段

#### 10.3 ECharts 图表封装
- **vue-echarts**：将 ECharts 封装为 Vue 3 组件
- **图表组件化**：每个维度一个独立分析组件（SpaceUsageAnalyze 等）

---

### 面试题 10-1：如果 picture 表有 1000 万条记录，你的统计查询如何优化？

**参考答案：**

**问题**：`GROUP BY` + `COUNT` 全表扫描 1000 万行，查询可能耗时数秒至数十秒

**优化方案**：

**短期（不改架构）**：
1. **覆盖索引**：`CREATE INDEX idx_cover ON picture(spaceId, userId, category, picSize)` 覆盖分析常用字段
2. **预聚合表**：定时任务（如每小时）将统计结果写入 `space_stats` 表，查询时读预聚合结果
3. **缓存统计结果**：分析结果缓存到 Redis（如 10 分钟过期）
4. **限制时间范围**：前端只允许查询近 30 天/90 天

**中期（引入新组件）**：
5. **Elasticsearch**：同步 Picture 数据到 ES，利用聚合查询（Aggregation）替代 MySQL GROUP BY
6. **列式存储**：ClickHouse / Doris 专门用于 OLAP 分析

**长期（架构演进）**：
7. **读写分离**：分析查询走只读从库
8. **数据分片**：按 spaceId 分表，分析任务并行查询各分片后汇总

**本项目当前**：数据量小（4 张表），直接 GROUP BY 即可。但架构上预留了 `SpaceAnalyzeApplicationService` 独立分析层，未来可无缝对接 ES。

---

## 阶段 11：团队空间模块

### 学习目标
实现团队空间管理、成员邀请/移除、空间角色变更、Sa-Token 空间鉴权。

### 详细知识点

#### 11.1 团队空间数据模型
- **space_user 表**：`spaceId + userId + spaceRole`，N:N 关系
- **空间角色**：viewer（浏览）、editor（编辑/上传）、admin（管理成员 + 编辑）

#### 11.2 Spring 循环依赖问题
- **问题**：`SpaceServiceImpl ↔ SpaceUserServiceImpl` 互相注入
- **解决方案**：`@Lazy` 注解 → 注入代理对象 → 首次调用时才真正获取目标 Bean
- **Spring 三级缓存**：
  - `singletonObjects`（一级）：成品 Bean
  - `earlySingletonObjects`（二级）：半成品 Bean（提前暴露引用）
  - `singletonFactories`（三级）：Bean 工厂（ObjectFactory）

---

### 面试题 11-1：Spring 是如何解决循环依赖的？@Lazy 的原理是什么？

**参考答案：**

**Spring 解决循环依赖的核心流程**（以 A ↔ B 为例）：

```
步骤 1: 创建 A
  → 实例化 A（调用构造器）
  → 将 A 的 ObjectFactory 放入三级缓存（singletonFactories）
  → 属性填充 → 发现需要 B

步骤 2: 创建 B
  → 实例化 B（调用构造器）
  → 将 B 的 ObjectFactory 放入三级缓存
  → 属性填充 → 发现需要 A
  → 从三级缓存获取 A 的 ObjectFactory → 创建 A 的早期引用
  → 将 A 的早期引用放入二级缓存（earlySingletonObjects）
  → B 完成属性填充 → 放入一级缓存（singletonObjects）

步骤 3: 回到 A
  → A 获得 B 的引用（从一级缓存）
  → A 完成属性填充 → 放入一级缓存
```

**为什么构造器注入无法解决循环依赖**：
- 构造器注入在"实例化"阶段就需要依赖 → 此时依赖对象还未创建 → 死循环

**@Lazy 原理**：
- 当字段标注 `@Lazy` 时，Spring 不直接注入目标 Bean，而是注入一个代理对象（CGLIB 代理）
- 只有首次调用该代理对象的方法时，代理才会从 Spring 容器中获取真正的 Bean
- 此时目标 Bean 已经完成初始化，所以不会出现循环依赖

**本项目中的应用**：
```java
@Service
public class SpaceServiceImpl {
    @Lazy  // 打破 SpaceServiceImpl → SpaceUserServiceImpl → SpaceServiceImpl 循环
    @Autowired
    private SpaceUserService spaceUserService;
}
```

---

## 阶段 12：图片协同编辑模块

### 学习目标
实现 WebSocket 双向通信、Disruptor 异步消息处理、编辑锁互斥、排除式广播。

### 详细知识点

#### 12.1 WebSocket 协议
- **与 HTTP 区别**：全双工、长连接、帧协议（非请求-响应）
- **握手**：HTTP Upgrade 请求 → `101 Switching Protocols` 响应
- **Spring WebSocket**：`WebSocketHandler` + `WebSocketConfigurer`
- **拦截器**：`HandshakeInterceptor` 在握手前校验权限

#### 12.2 Disruptor 环形缓冲区
- **核心数据结构**：RingBuffer（预分配数组，无 GC 压力）
- **核心组件**：
  - `RingBuffer`：存储事件
  - `Sequence`：序列号（跟踪生产者和消费者的进度）
  - `SequenceBarrier`：协调生产者和消费者
  - `EventProcessor`：消费事件
- **无锁设计**：CAS 操作替代锁
- **伪共享消除**：`@Contended` / 缓存行填充（Padding），确保 Sequence 独占缓存行

#### 12.3 Disruptor vs 线程池 vs MQ
| 维度 | Disruptor | 线程池 | MQ（RabbitMQ/Kafka） |
|------|----------|-------|---------------------|
| 延迟 | 微秒级 | 毫秒级 | 毫秒~秒级 |
| 吞吐量 | 极高（无锁） | 受锁竞争限制 | 高（但有网络开销） |
| 持久化 | 无（内存） | 无 | 有 |
| 适用场景 | 同进程超高吞吐 | 通用异步 | 分布式/跨服务 |

#### 12.4 协同编辑设计
- **编辑锁**：`ConcurrentHashMap<pictureId, editingUserId>`，`putIfAbsent` 原子操作
- **排除式广播**：操作者本身不接收广播（本地已即时执行），避免重复渲染
- **前端即时反馈**：编辑操作先更新本地 UI → 再通过 WebSocket 发送 → 其他客户端同步

#### 12.5 等待策略对比
| 策略 | CPU占用 | 延迟 | 适用场景 |
|------|--------|------|---------|
| BlockingWaitStrategy | 最低 | 最高 | 对延迟不敏感 |
| SleepingWaitStrategy | 中等 | 中等 | 折中选择 |
| YieldingWaitStrategy | 较高 | 低 | 低延迟场景 |
| BusySpinWaitStrategy | 100% | 最低 | 极致延迟 |

---

### 面试题 12-1：WebSocket 和 HTTP 长轮询、SSE 有什么区别？

**参考答案：**

| 维度 | HTTP 长轮询 | SSE | WebSocket |
|------|-----------|-----|-----------|
| 方向 | 双向（伪） | 单向（服务端→客户端） | 真正的全双工 |
| 协议 | HTTP | HTTP | WS/WSS（独立协议） |
| 连接 | 每次请求/响应后断开 | 保持连接 | 保持连接 |
| 浏览器支持 | 全部 | 大部分（IE 不支持） | 全部现代浏览器 |
| 服务端实现 | 简单 | 简单 | 相对复杂 |
| 适用场景 | 兼容性要求高 | 实时推送（股票/通知） | 双向实时（聊天/协同） |

**本项目为什么选 WebSocket**：
- 协同编辑需要双向实时通信（客户端发编辑操作、服务端广播给其他人）
- SSE 只支持服务端 → 客户端单向推送，无法满足
- 长轮询延迟高、资源浪费

---

### 面试题 12-2：Disruptor 为什么比线程池快？核心设计思想是什么？

**参考答案：**

**Disruptor 快的核心原因**：

**1. 无锁设计（CAS）**
- 生产者和消费者通过 `Sequence`（序列号）+ CAS 原子操作协调
- 避免了传统阻塞队列的 `synchronized` / `ReentrantLock` 锁竞争

**2. RingBuffer 预分配内存**
- 数组在创建时一次性分配所有槽位
- 事件对象复用（创建后不清除，覆盖写入）
- **零 GC 压力**：运行期间不会产生新对象

**3. 伪共享（False Sharing）消除**
- CPU 缓存以缓存行（Cache Line，通常 64 字节）为单位加载
- 如果两个变量在同一缓存行，修改一个会导致另一个变量的缓存失效
- Disruptor 的 `Sequence` 通过前后填充 7 个 long（56 字节 + 8 = 64 字节）独占缓存行

**4. 单生产者优化**
- 多生产者需要 CAS 竞争写位置
- 单生产者模式（`SingleProducerSequencer`）无需 CAS，直接递增 Sequence

**与线程池对比**：
- 线程池 + 阻塞队列 → `ArrayBlockingQueue.put()` 使用 `ReentrantLock` → 锁竞争 + 线程上下文切换
- Disruptor → CAS + 内存屏障 → 用户态完成，无系统调用

---

### 面试题 12-3：如果 WebSocket 服务部署到多个实例，如何保证消息广播到所有实例的客户端？

**参考答案：**

**问题**：用户 A 在实例 1 上，用户 B 在实例 2 上，A 的编辑操作如何广播给 B？

**方案一：Redis Pub/Sub**
- 每个实例启动时订阅同一个 Redis Channel
- 实例 1 收到消息 → 本地处理 + 发布到 Redis
- 实例 2 的订阅者收到消息 → 广播给本地连接的客户端
- 优点：实现简单、无额外组件
- 缺点：Redis Pub/Sub 不持久化（消息丢失风险）

**方案二：消息队列（RabbitMQ / Kafka）**
- 使用 Fanout Exchange（RabbitMQ）或广播模式（Kafka）
- 每个实例订阅队列 → 收到消息后广播给本地客户端
- 优点：消息持久化、可靠投递
- 缺点：引入额外组件，增加运维复杂度

**方案三：一致性 Hash 路由**
- Nginx / Gateway 根据 spaceId 做一致性 Hash
- 同一空间的用户始终路由到同一实例
- 优点：无需消息中间件
- 缺点：实例上下线会导致重分配、负载不均

**本项目**：当前单实例部署，如果扩展到多实例，推荐 Redis Pub/Sub 方案（改动最小、依赖已有 Redis）。

---

## 阶段 13：DDD 架构重构

### 学习目标
理解 DDD 核心概念、掌握四层架构设计、完成从三层到四层的重构。

### 详细知识点

#### 13.1 DDD 核心概念
- **有界上下文（Bounded Context）**：领域的边界划分，本项目 4 个：User / Picture / Space / SpaceUser
- **实体（Entity）**：有唯一标识的对象（User、Picture、Space）
- **值对象（Value Object）**：无唯一标识，通过属性值定义相等性（SpaceTypeEnum、UserRoleEnum）
- **聚合（Aggregate）**：实体和值对象的集合，通过聚合根访问（Picture 聚合、Space 聚合）
- **领域服务（DomainService）**：不适合放在 Entity 中的领域逻辑
- **仓储（Repository）**：领域层的持久化接口，由 Infrastructure 层实现

#### 13.2 DDD 四层架构
```
interfaces（接口层）
│  Controller / DTO / VO / Assembler
│  ↓ 依赖
application（应用层）
│  ApplicationService（流程编排 / 事务管理）
│  ↓ 依赖
domain（领域层）← 核心，不依赖任何框架
│  Entity / ValueObject / DomainService / Repository接口
│  ↑ 实现
infrastructure（基础设施层）
   RepositoryImpl / Mapper / COS / Redis / AI API
   
shared（横切层）
   Auth / WebSocket / 跨域共享代码
```

#### 13.3 依赖倒置
- **Domain 层定义接口**：`PictureRepository extends IService<Picture>`
- **Infrastructure 层实现接口**：`PictureRepositoryImpl extends ServiceImpl<PictureMapper, Picture> implements PictureRepository`
- **核心思想**：高层模块（Domain）不依赖低层模块（Infrastructure），二者都依赖抽象

#### 13.4 ApplicationService vs DomainService 职责边界
- **ApplicationService**：业务流程编排、事务管理、跨域调用协调（如上传涉及 COS + 压缩 + 配额更新）
- **DomainService**：单一领域内的纯业务逻辑（如 Picture 的审核状态变更规则）
- **判断标准**：不涉及跨服务调用 → 下沉到 DomainService；需要编排多个 DomainService → Application 层

#### 13.5 充血模型 vs 贫血模型
- **贫血模型**：Entity 只有 getter/setter，业务逻辑全在 Service
- **充血模型**：Entity 包含自身行为方法（如 `Picture.approve()`）
- **平衡**：简单逻辑放 Entity，复杂跨实体逻辑放 DomainService

---

### 面试题 13-1：DDD 和传统三层架构的本质区别是什么？什么样的项目适合 DDD？

**参考答案：**

**本质区别**：
- **三层架构**：以数据库为中心（数据驱动），Controller → Service → Mapper，Service 层容易膨胀成"大泥球"
- **DDD**：以领域模型为中心（领域驱动），代码组织围绕业务概念而非技术分层

**DDD 的核心价值**：
1. **业务逻辑内聚**：图片相关的所有代码在 Picture 领域内，修改不会波及 User 领域
2. **依赖方向可控**：所有依赖指向 Domain 层（依赖倒置），Domain 层可独立测试
3. **代码即文档**：目录结构直接反映业务概念（domain/picture/entity/Picture）

**DDD 适合的场景**：
- 业务规则复杂（多个状态流转、复杂的权限逻辑）
- 多个有界上下文需要清晰边界
- 长期维护的项目（业务会持续演进的）

**DDD 不适合的场景**：
- 简单 CRUD 系统（无复杂业务规则）
- 快速原型验证
- 团队对 DDD 不熟悉（学习成本高）

**本项目的适合性**：4 个有界上下文、RBAC 权限体系、多种空间模式、协同编辑，业务复杂度适中，适合 DDD。但文件数量确实增加了，简单查询路径变长了。

---

### 面试题 13-2：Repository 模式和 DAO 模式有什么区别？

**参考答案：**

| 维度 | DAO（Data Access Object） | Repository |
|------|--------------------------|-----------|
| 关注点 | 数据访问技术细节 | 领域对象的集合管理 |
| 层次 | 持久化层 | 领域层（接口）+ 基础设施层（实现） |
| 粒度 | 单表操作 | 聚合根操作 |
| 返回值 | 原始数据（Entity / List） | 领域对象（可包含业务含义） |
| 依赖方向 | Service → DAO | Domain ← Infrastructure |

**核心区别**：
- **DAO** 是从数据库角度设计的（一张表一个 DAO），接口反映数据库结构
- **Repository** 是从领域角度设计的（一个聚合一个 Repository），接口反映业务语义

**示例对比**：
```java
// DAO 风格：暴露数据库操作细节
User findById(Long id);
void updateSelective(User user);
List<User> findByStatus(Integer status);

// Repository 风格：以领域语言表达
Optional<User> findById(UserId id);
void save(User user);
List<User> findActiveUsers();
```

**本项目中的实现**：
```java
// Domain 层定义接口（领域语义）
public interface PictureRepository extends IService<Picture> {
    // IService 来自 MyBatis-Plus，提供 CRUD 基础方法
}

// Infrastructure 层实现
@Repository
public class PictureRepositoryImpl 
    extends ServiceImpl<PictureMapper, Picture> 
    implements PictureRepository {
}
```

---

### 面试题 13-3：你在 DDD 重构过程中，最纠结的一个技术决策是什么？

**参考答案：**

最纠结的是 **"是否所有查询都需要走 DomainService"**。

**场景**：首页图片列表查询（分页 + 多条件筛选 + 排序），这是一个纯查询操作，没有任何业务规则。

**两种方案**：
- 方案 A（严格 DDD）：查询也走 DomainService → ApplicationService → Controller。好处是架构一致性，代价是简单的查询也要经过 3 层。
- 方案 B（务实折中）：简单查询由 ApplicationService 直接调用 Repository/IService 方法完成，DomainService 只处理有业务规则的写操作。

**最终决策**：选择了方案 B。理由是 DDD 的目的是让复杂业务更清晰，而不是为了架构纯净性把简单事情变复杂。简单查询直接通过 MyBatis-Plus 的 `IService` 方法完成即可。

**判断标准**：
- 如果查询包含业务规则（如"用户只能看到自己空间 或 公共图库中审核通过的图片"） → 走 DomainService
- 如果查询是纯数据检索（如"所有审核通过的图片按时间倒序"） → ApplicationService 直接操作

**反思**：过度设计 DDD 同样有害。好的架构是在"纯粹性"和"实用性"之间找到平衡。

---

## 阶段 14：部署上线

### 学习目标
前端部署到 Cloudflare Pages、后端通过 Cloudflare Tunnel 实现公网访问、生产环境配置。

### 详细知识点

#### 14.1 Cloudflare Pages 部署
- **连接 Git 仓库**：自动检测框架（Vite）、构建命令 `npm run build`
- **输出目录**：`dist/`
- **环境变量**：`VITE_API_BASE_URL` 指定后端地址
- **自动 HTTPS**：免费 SSL 证书

#### 14.2 Cloudflare Tunnel 内网穿透
- **原理**：在本地机器运行 `cloudflared` 守护进程 → 建立到 Cloudflare 边缘节点的加密隧道 → 公网域名映射到本地端口
- **命令**：`cloudflared tunnel --url http://localhost:8123`

#### 14.3 生产环境配置
- **跨域配置**：生产环境 CorsConfig 添加实际域名
- **WebSocket wss://**：生产环境使用 `wss://` 协议（等价于 HTTPS）
- **前端 baseURL**：从 `import.meta.env.VITE_API_BASE_URL` 读取（环境变量注入）

#### 14.4 部署常见问题
- **构建命令错误**：`npm build` → `npm run build`（缺少 `run`）
- **baseURL 硬编码**：本地 `localhost:8123` → 生产环境变量

---

### 面试题 14-1：Cloudflare Tunnel 和传统 Nginx 反向代理有什么区别？

**参考答案：**

| 维度 | Cloudflare Tunnel | Nginx 反向代理 |
|------|------------------|---------------|
| 部署方式 | 本地运行 cloudflared | 需要公网服务器 |
| 网络要求 | 只要能出站即可（不需公网 IP） | 需要公网 IP 或端口映射 |
| 安全 | Cloudflare 边缘节点过滤攻击 | 靠自己配置防护 |
| HTTPS | 自动免费 SSL | 需要手动申请证书 |
| 成本 | 免费（Cloudflare Free 计划） | 需要服务器成本 |
| 适用场景 | 个人项目 / 小型应用 | 中大型生产环境 |

**本项目为什么用 Cloudflare Tunnel**：
- 没有公网服务器，也不需要买
- 项目体量小（个人项目），不需要 Nginx 的反向代理/负载均衡能力
- 免费 HTTPS，零运维成本

**如果升级到生产级**：应使用 Nginx + Docker + 云服务器，实现负载均衡、静态资源缓存、限流等。

---

### 面试题 14-2：如何实现前端部署的零停机（Zero Downtime）？

**参考答案：**

**Cloudflare Pages 天然支持零停机**：
- 每次部署生成一个新的预览 URL
- 部署成功后将新版本流量逐步切过去
- 旧版本保持不变直到新版本验证通过

**通用零停机部署策略**：
1. **蓝绿部署（Blue-Green）**：维护两套环境，切换 DNS/负载均衡指向新版
2. **滚动部署（Rolling）**：逐个实例替换，始终保持部分实例在线
3. **金丝雀发布（Canary）**：先切 5% 流量到新版，验证无误后逐步增加到 100%

**前端特别注意**：
- 静态资源使用 Hash 文件名（`app.abc123.js`），新旧版本不冲突
- CDN 缓存策略：HTML 短缓存或不缓存，JS/CSS 长缓存
- 后端 API 版本化（`/api/v1/`），确保新版前端匹配新版 API

---

## 阶段 15：附加 — Snowflake ID 精度丢失专项

### 学习目标
理解 JavaScript Number 精度限制、Snowflake 算法原理、前后端 Long 类型安全实践。

### 详细知识点

#### 15.1 JavaScript Number 精度
- **IEEE 754 双精度浮点数**：64 位 = 1（符号位）+ 11（指数位）+ 52（尾数位）
- **安全整数范围**：`Number.MAX_SAFE_INTEGER` = `2^53 - 1` = `9007199254740991`（约 16 位）
- **Snowflake ID**：19 位 Long 型（如 `1801234567890123456`），超出 JS 安全整数范围
- **精度丢失现象**：`1801234567890123456` → `Number` → `1801234567890123400`

#### 15.2 Snowflake 算法
- **结构**：1 位（保留）+ 41 位（毫秒时间戳）+ 10 位（机器 ID）+ 12 位（序列号）= 64 位
- **优势**：全局唯一、趋势递增、本地生成（无网络调用）
- **本项目配置**：MyBatis-Plus `IdType.ASSIGN_ID` 自动使用 Snowflake

#### 15.3 全链路修复方案
1. **后端 → 前端**：`JsonConfig` 中所有 Long 类型使用 `ToStringSerializer` 序列化为 String
2. **前端 → 后端**：所有 ID 参数类型 `number` → `string | number`，保持 String 传递
3. **Spring MVC 自动解析**：接收 String 参数 → 自动转换为 Long（`StringToLongConverter`）

---

### 面试题 15-1：JavaScript 中为什么 `0.1 + 0.2 !== 0.3`？这和 Snowflake ID 精度丢失是同一个原因吗？

**参考答案：**

**0.1 + 0.2 的问题**：
- IEEE 754 浮点数用二进制存储小数，0.1 和 0.2 在二进制中是无限循环小数
- 存储时被截断，产生舍入误差
- `0.1 + 0.2` 实际上是 `0.30000000000000004`

**Snowflake ID 精度丢失**：
- 17+ 位的整数超出了 JS 52 位尾数的表示范围
- 超出部分被舍入（Rounding），末尾几位变成 0

**两者相同点**：根因都是 IEEE 754 的精度限制

**两者不同点**：
- `0.1 + 0.2` 是小数表示的精度问题（计算中的误差）
- Snowflake ID 是**大整数无法精确表示**的问题（超出 52 位尾数）

**解决方案对比**：
- 小数精度：用 `toFixed()` / `Math.round()` / 转换为整数（分）计算
- 大整数：用 `String` 传输 / `BigInt` / `json-bigint` 库

---

## 阶段 16：附加 — 模板方法模式深度解析

### 面试题 16-1：请画出模板方法模式的类图，并解释"骨架方法"和"钩子方法"的区别。

**参考答案：**

```
abstract PictureUploadTemplate {
    + uploadPicture(inputSource)  ← 骨架方法（final，定义步骤，不可覆盖）
    # validateFile(inputSource)   ← 钩子方法（abstract / protected，子类覆盖）
    # createTempFile(inputSource)
    # processPicture(file)
    # uploadToCOS(file)
    # buildResult(uploadResult)
    # cleanup(file)
}

FilePictureUpload extends PictureUploadTemplate {
    # validateFile(MultipartFile file)     → 校验文件大小/类型
    # createTempFile(MultipartFile file)   → 从 MultipartFile 创建临时文件
    ...
}

UrlPictureUpload extends PictureUploadTemplate {
    # validateFile(String url)             → 校验 URL 格式
    # createTempFile(String url)           → 从 URL 下载创建临时文件
    ...
}
```

**骨架方法**（Template Method）：
- `uploadPicture()` 定义了上传的**固定算法步骤**：校验 → 创建临时文件 → 处理 → 上传 → 构建结果 → 清理
- 用 `final` 修饰，**不允许子类覆盖**
- 保证了上传流程的一致性

**钩子方法**（Hook Method）：
- `validateFile()`、`createTempFile()` 等是可覆盖的步骤
- 用 `protected abstract` 或 `protected`（提供默认实现）
- 子类根据自己的需求实现具体逻辑

**收益**：
1. **复用**：6 步流程代码只需写一次（父类 150 行）
2. **扩展**：新增上传方式只需新增子类，无需修改已有代码（开闭原则）
3. **一致性**：所有上传方式遵循相同的流程规范

---

# 第四部分：学习计划建议

## 4.1 14 天学习计划

```
第 1 天  │ 阶段 1 + 2   │ 项目总览 + 环境搭建          │ 理解业务全貌，搭好前后端脚手架
第 2 天  │ 阶段 3       │ 用户模块                    │ 注册/登录/Session/异常处理
第 3 天  │ 阶段 4       │ 图片模块                    │ CRUD/COS上传/万象CI
第 4 天  │ 阶段 5 + 6   │ 传图 + 缓存优化             │ 批量抓图/两级缓存
第 5 天  │ 阶段 7       │ 空间模块                    │ 空间CRUD/权限配置
第 6 天  │ 阶段 8       │ 图片功能扩展                │ 编辑/下载/裁剪
第 7 天  │ 阶段 9       │ AI图片编辑                  │ 百炼AI/异步任务/轮询
第 8 天  │ 阶段 10      │ 图库分析                    │ 6维分析/ECharts
第 9 天  │ 阶段 11      │ 团队空间                    │ 成员管理/RBAC鉴权
第 10 天 │ 阶段 12      │ 协同编辑                    │ WebSocket/Disruptor
第 11 天 │ 阶段 13      │ DDD重构                     │ 四层架构/依赖倒置
第 12 天 │ 阶段 14      │ 部署上线                    │ Cloudflare/Tunnel
第 13 天 │ 复习         │ 全面复盘 + 面试题练习        │ 每个阶段的面试题都要能答
第 14 天 │ 模拟面试     │ 3 小时模拟面试              │ 按面试考察文档练习
```

## 4.2 学习策略建议

1. **动手优先**：每个阶段读完文档后，必须自己敲一遍代码，不要复制粘贴
2. **Bug 复盘**：Bug 记录文档中的 45 个 Bug，逐一理解根因和修复思路
3. **面试题练习**：每天抽 1 小时练习当天阶段的面试题
4. **画图理解**：架构图、流程图、类图——画出来才算真正理解了
5. **追问自己**：每完成一个阶段，问自己"为什么这样设计？""有没有更好的方案？"

---

# 第五部分：面试快速查阅卡片

## 高频面试题 TOP 15

| 序号 | 面试题 | 对应阶段 |
|:---:|--------|:---:|
| 1 | 3 分钟项目概述 | 阶段 1 |
| 2 | DDD 四层架构的依赖关系 | 阶段 13 |
| 3 | ApplicationService 和 DomainService 职责边界 | 阶段 13 |
| 4 | 缓存穿透/击穿/雪崩 区别 | 阶段 6 |
| 5 | Session-Cookie vs JWT vs Sa-Token | 阶段 3/7 |
| 6 | Spring 循环依赖解决机制（三级缓存） | 阶段 11 |
| 7 | Disruptor 为什么快 | 阶段 12 |
| 8 | RBAC 权限模型落地实践 | 阶段 7 |
| 9 | 模板方法模式 | 阶段 15 |
| 10 | Snowflake ID 精度丢失根因 | 阶段 14 |
| 11 | WebSocket vs SSE vs 长轮询 | 阶段 12 |
| 12 | 图片上传安全校验 | 阶段 4 |
| 13 | 异步任务 vs 同步模式 | 阶段 9 |
| 14 | 缓存一致性方案 | 阶段 6 |
| 15 | 项目重构经验与决策 | 阶段 13 |

---

> 📌 **使用说明**：
> 1. 按学习计划每天完成 1-2 个阶段
> 2. 每完成一个阶段，回答该阶段的面试题（口头练习，推荐录音回听）
> 3. 全部完成后进行 3 小时模拟面试
> 4. 面试前重点复习"高频面试题 TOP 15"
