2026/6/21
1.MySQl数据库初始化失败
问题：MySQL数据库初始化失败
解决：root用户的密码为@087174Ab
https://blog.csdn.net/flywing521/article/details/149025193

2.安装好mybatis-plus后为什么可以删除mybatis的依赖
添加maven插件：maven helper观察项目依赖
mybatis-plus的分页插件部分在3.5.9版本更新了，不删除原有的mybatis依赖项会在后续的开发中造成冲突
通过maven helper插件观察项目依赖可以看到mybatis-plus插件是包含了mybatis的

3.工具库HuTools
链接：https://doc.hutool.cn/pages/ai/

4.接口文档工具
swagger:https://swagger.io/
knife4j:https://doc.xiaominfo.com/docs/quick-start
在knife4j中存在OpenAPI规范：即生成的接口文档所需要遵守的数据规范
了解接口文档组件knife4j的引入和运行流程，重启项目获取接口文档的地址：http://localhost:8123/api/doc.html#/home

5.springboot的面向切面编程{AOP}

6.编写通用代码
第一部分，自定义异常
归纳处理异常类型，自定义一套错误码

2026/6/23：用户模块
1.用户模块的需求分析
用户注册
用户登录
获取当前登录用户
用户注销
用户权限控制
【管理员】管理用户


2.创建mySQL表格
报错：DBMS: MySQL (无版本)
区分大小写: 普通形式=mixed，分隔形式=exact
Connection refused: connect.

报错原因：mySQL服务未启动，需要在命令控制台输入services.msc，启动mySQL服务

3.拓展功能思考
添加会员字段，会员日期时间，用户的邀请等等

4.了解用户的登录流程
session或者token模式

5.如何控制用户权限
分析四种用户权限

6.如何校验权限
通过spring AOP切面编程实现权限的校验

7.mybatisX插件


2026/6/24
1.用户模块的注册接口昨天没有推送，造成所有进度失效，今天重新做

2.创建包的子包会连在一起
点击项目右边的三点设置项目，点击外观，取消压缩空的中间包选项即可

3.开发用户模块。用户的注册/登录与注销

4.开发用户的增删改查模块

5.分页查询
mybatis-plus的3.5.9版本关于分页部分进行了更新，导致我们编写的分页查询代码未生效
解决：配置MybatisPlusConfig，注册PaginationInnerInterceptor分页插件，指定DbType.MYSQL
注意：如果配置多个插件，分页插件必须最后添加
配置文件：com.vio.aitukuviobe.config.MybatisPlusConfig

6.数据精度类型
前端js的数据精度小于后端lang类型
在项目中使用全局精度进行解决：com.vio.aitukuviobe.config.JsonConfig
原理：Jackson序列化时，将Long类型统一转为String，避免前端Number精度丢失（JS的Number最大安全整数为2^53-1，而Java Long可达2^63-1）

7.用户实体设计
User实体位于com.vio.aitukuviobe.model.entity.User
主键策略：ASSIGN_ID（雪花算法生成Long型ID）
包含字段：账号、密码、昵称、头像、简介、角色(user/vip/admin)、会员过期时间、会员兑换码、会员编号、编辑时间、创建时间、更新时间、逻辑删除标记
使用@TableLogic注解实现逻辑删除（isDelete字段）
密码存储使用MD5加盐加密，盐值为"yupi"

8.用户注册接口 POST /api/user/register
流程：校验参数（非空、长度、两次密码一致性）→ 检查账号重复 → 密码MD5加盐加密 → 插入数据库 → 返回新用户ID
默认昵称："无名"
默认角色：user
请求体：UserRegisterRequest（userAccount, userPassword, checkPassword）

9.用户登录接口 POST /api/user/login
流程：参数校验 → 密码加密 → 数据库匹配账号密码 → 存入Session → 返回脱敏用户信息
登录态通过HttpSession维护，key为UserConstant.USER_LOGIN_STATE
返回LoginUserVO（不含密码等敏感字段）
请求体：UserLoginRequest（userAccount, userPassword）

10.用户注销 POST /api/user/logout
流程：检查登录态 → 移除Session中的用户信息

11.获取当前登录用户 GET /api/user/get/login
从Session中获取当前登录用户，返回脱敏后的LoginUserVO

12.用户增删改查（管理员权限）
新增用户 POST /api/user/add：设置默认密码12345678，密码加密后入库
删除用户 POST /api/user/delete：逻辑删除（isDelete标记）
修改用户 POST /api/user/update：支持更新用户信息
查询用户 GET /api/user/get：根据ID获取用户
分页查询 POST /api/user/list/page/vo：支持多条件筛选（账号、昵称、角色、简介模糊匹配），返回Page<UserVO>分页结果

13.用户角色与权限控制
角色枚举：UserRoleEnum（USER/VIP/ADMIN），位于com.vio.aitukuviobe.model.enums
权限常量：UserConstant中定义DEFAULT_ROLE="user"、VIP_ROLE="vip"、ADMIN_ROLE="admin"
使用自定义注解@AuthCheck(mustRole)标记需要权限的接口
通过AOP切面AuthInterceptor拦截@AuthCheck注解，从Session获取当前用户，比对角色是否满足要求
不需要权限时mustRole为空字符串，直接放行

14. 统一响应封装
BaseResponse<T>：通用响应体，包含code（状态码）、data（数据）、message（消息）
ResultUtils：静态工具类，提供success()和error()方法快速构建响应
成功响应：code=0, message="ok"
错误响应：使用ErrorCode枚举（PARAMS_ERROR 40000, NOT_LOGIN_ERROR 40100, NO_AUTH_ERROR 40101, NOT_FOUND_ERROR 40400, FORBIDDEN_ERROR 40300, SYSTEM_ERROR 50000, OPERATION_ERROR 50001）

15.全局异常处理
BusinessException：自定义业务异常，携带ErrorCode和自定义message
GlobalExceptionHandler：@RestControllerAdvice全局异常拦截，分别处理BusinessException和RuntimeException
ThrowUtils：断言工具类，条件成立则抛出BusinessException，简化参数校验代码

16.数据脱敏（VO模式）
LoginUserVO：登录用户视图，不包含密码，用于返回给登录用户自己的信息
UserVO：用户视图，不包含密码，额外包含VIP相关字段（vipExpireTime, vipCode, vipNumber）
通过BeanUtil.copyProperties进行实体到VO的转换

17.跨域配置
CorsConfig：允许所有来源、所有方法、所有Header的跨域请求，支持Cookie传递

18.今日Bug修复记录
① pom.xml中mybatis-plus-bom版本3.5.9与boot-starter版本3.5.15不一致，导致ClassNotFoundException: MybatisPlusApplicationContextAware → 统一BOM版本为3.5.15
② application.yml中global-config缩进错误，嵌套在configuration下 → 移到与configuration同级
③ UserMapper.xml中namespace和resultMap引用了代码生成器遗留的包路径com.vio.aitukuviobe.generator.* → 修正为实际路径com.vio.aitukuviobe.mapper.UserMapper和com.vio.aitukuviobe.model.entity.User
④ 删除src/main/resources下的com/和generator/冗余XML目录，避免Mapper扫描冲突
⑤ 用户登录失败："系统错误" —— 注册成功但使用相同账号密码登录报系统错误

问题现象：使用注册页面成功注册账号（如vio001/12345678），但使用相同账号密码在登录页面登录时提示"登录失败，系统错误"。

根因分析：
- User实体（com.vio.aitukuviobe.model.entity.User）中定义了vipExpireTime、vipCode、vipNumber三个字段
- 但数据库user表中并没有这些列（SQL建表时尚未包含会员相关字段）
- 注册时使用selectCount（SELECT COUNT(*)）和save（INSERT仅包含非null字段），不会触发缺失列的问题
- 登录时使用selectOne，MyBatis-Plus生成SQL会SELECT实体类的全部字段，包括不存在的vipExpireTime、vipCode、vipNumber
- MySQL报"Unknown column 'vipExpireTime'" → BadSqlGrammarException（RuntimeException）→ GlobalExceptionHandler捕获返回code=50000, message="系统错误"

解决方案：在User实体的vipExpireTime、vipCode、vipNumber三个字段上添加@TableField(exist = false)注解，告诉MyBatis-Plus这些字段不对应数据库列，SELECT时不再包含。

涉及文件：com.vio.aitukuviobe.model.entity.User（第55/61/67行）

2026/6/25
1.添加MySQL的管理员和普通用户的数据，测试用户模块的正确性
但还需要系统学习IDEA中如何使用MySQL

2.进行图片模块的编写
本项目中使用腾讯云的COS，需要区分API和SDK的区别，API直接使用网络调用即可，但是SDK封装了API，需要下载到本地使用
3.学习数据对象的存储桶（COS Bucket）
腾讯云COS对象存储概念：存储桶（Bucket）是对象的容器，对象（Object）是存储的基本单元
SDK方式：引入cos_api依赖（com.qcloud:cos_api:5.6.227），通过COSClient操作对象
API方式：直接HTTP调用COS接口，无需本地依赖

4.COS SDK依赖选型
问题：最初引入了tencentcloud-sdk-java（3.1.1491）作为COS依赖
解决：该项目实际使用的是cos_api（5.6.227），二者不同。cos_api是专门的对象存储SDK，tencentcloud-sdk-java是腾讯云全产品SDK。替换为正确的依赖。

5.创建picture数据库表
建表SQL位置：AI-TUKU-VIO-BE/sql/create_table.sql
picture表字段：id, url, name, introduction, category, tags, picSize, picWidth, picHeight, picScale, picFormat, userId, createTime, editTime, updateTime, isDelete
索引：idx_name, idx_introduction(前缀索引191), idx_category, idx_tags(前缀索引191), idx_userId
使用utf8mb4_unicode_ci编码，支持emoji等特殊字符
逻辑删除字段isDelete配合MyBatis-Plus @TableLogic实现软删除

2026/6/26：图片模块后端开发

1.图片模块架构概览
Controller层：
  - FileController：文件上传/下载测试接口（POST /api/file/test/upload, GET /api/file/test/download/）
  - PictureController：图片业务接口（上传、删除、更新、查询、编辑、标签分类）
Config层：
  - CosClientConfig：腾讯云COS客户端配置，@ConfigurationProperties(prefix = "cos.client")绑定配置文件
Manager层（可复用业务逻辑层）：
  - CosManager：COS对象存储管理器（putObject上传、getObject下载、putPictureObject上传带图片处理、deleteObject删除）
  - FileManager：文件管理器（uploadPicture上传图片+校验、deleteTempFile清理临时文件）——已标记@Deprecated，计划用模板方法重构
Service层：
  - PictureService接口 + PictureServiceImpl实现：图片核心业务逻辑
Model层：
  - entity/Picture：图片实体（@TableName "picture"），包含审核、空间等字段
  - vo/PictureVO：图片视图对象（含关联UserVO、权限列表permissionList）
  - vo/PictureTagCategory：标签分类视图
  - dto/picture/PictureUploadRequest：上传请求DTO
  - dto/picture/PictureUpdateRequest：更新请求DTO
  - dto/picture/PictureEditRequest：编辑请求DTO
  - dto/picture/PictureQueryRequest：查询请求DTO（继承PageRequest，支持多条件筛选+排序）
  - dto/file/UploadPictureResult：上传结果DTO

2.COS对象存储配置
配置文件：cos.client.host, secretId, secretKey, region, bucket
敏感信息（secretId/secretKey）存放在application-local.yml中，不提交到版本控制
需要在application.yml中设置spring.profiles.active: local来激活本地配置
CosClientConfig通过@ConfigurationProperties注解自动绑定配置，@Bean方法创建COSClient实例

3.CosManager — COS对象操作封装
putObject(key, file)：上传文件到COS
getObject(key)：下载COS对象
putPictureObject(key, file)：上传图片并附带万象CI处理
  - 获取原图信息（isPicInfo=1）
  - 图片压缩转webp格式（imageMogr2/format/webp）
  - 对>20KB的图片生成256x256缩略图（imageMogr2/thumbnail/256x256>）
deleteObject(key)：删除COS对象（用于图片删除时清理云端文件）

4.FileManager — 文件上传与校验
uploadPicture流程：
  ① 校验文件（尺寸≤2MB，后缀限定jpeg/png/jpg/webp）
  ② 生成唯一文件名（日期_UUID.后缀）防止冲突
  ③ 上传至COS的public/{userId}/目录，按用户隔离
  ④ 调用CosManager.putPictureObject获取图片信息（宽高、格式等）
  ⑤ 封装UploadPictureResult返回
validPicture校验：
  文件非空 → 大小≤2MB → 后缀白名单（jpeg/png/jpg/webp）
临时文件处理：上传完成后在finally块中删除临时文件

5.PictureServiceImpl — 图片核心业务
validPicture：校验id非空、url长度≤1024、简介长度≤800
uploadPicture：支持新增和更新两种模式
  - 通过pictureUploadRequest.id判断：有id为更新，无id为新增
  - 管理员自动过审（reviewStatus=1），普通用户待审核（reviewStatus=0）
  - 使用saveOrUpdate实现新增/更新合一
getPictureVO：Picture→PictureVO转换，关联查询用户信息
getPictureVOPage：分页VO转换，批量关联查询用户信息（一次查询所有userId，避免N+1问题）
getQueryWrapper：构建动态查询条件，支持搜索文本（name+introduction模糊匹配）、标签JSON模糊查询、排序
editPicture：用户编辑图片，管理员编辑自动过审，普通用户编辑后重置为待审核
deletePicture：逻辑删除数据库记录 → @Async异步清理COS文件
clearPictureFile：异步清理，删除前检查同一url是否被其他记录引用（count>1则跳过），同时删除缩略图
checkPictureAuth：权限校验
  - 公共图库（spaceId=null）：本人或管理员可操作
  - 私有空间（spaceId≠null）：仅本人可操作

6.PictureController — 图片接口
POST /api/picture/upload —— 上传图片（管理员）
POST /api/picture/delete —— 删除图片（管理员）
POST /api/picture/update —— 更新图片（管理员），实体与DTO转换，tags List→JSON String
GET  /api/picture/get —— 根据ID获取图片（管理员）
GET  /api/picture/get/vo —— 根据ID获取图片VO（含用户信息+权限校验）
POST /api/picture/list/page —— 分页查询图片（管理员）
POST /api/picture/list/page/vo —— 分页查询图片VO（公开图库，仅展示审核通过&公共空间图片，限制每页20条防爬虫）
POST /api/picture/edit —— 编辑图片（管理员）
GET  /api/picture/tag_category —— 获取预设标签分类列表

7.今日Bug修复记录

① COS SDK依赖错误
问题：pom.xml中引入了tencentcloud-sdk-java（全产品SDK），但实际代码使用的是cos_api的API
现象：import com.qcloud.cos.* 找不到类
解决：将依赖替换为 com.qcloud:cos_api:5.6.227

② PictureMapper、Picture实体、PictureService/Impl 在开发提交时遗漏
问题：b8af121提交开发了图片模块（Controller、Config、Manager、DTO、VO），但漏掉了Entity/Mapper/Service核心层
解决：补充提交Picture.java、PictureMapper.java、PictureService.java、PictureServiceImpl.java、PictureMapper.xml
这是最关键的bug fix，缺少这些文件整个图片模块无法编译运行

③ Picture实体字段与数据库表不匹配
问题：Picture实体中定义了thumbnailUrl、picColor、spaceId、reviewStatus、reviewMessage、reviewerId、reviewTime等字段，但create_table.sql中的picture表没有这些列
影响：运行时MyBatis-Plus会生成包含不存在列的SQL → Unknown column错误（类似之前User实体vipExpireTime的问题）
待解决：需要在picture表中ALTER TABLE添加这些缺失的列，或给实体字段添加@TableField(exist = false)

④ PictureMapper.xml 重复扫描导致Bean冲突
问题：PictureMapper.xml放在src/main/resources/generator/mapper/目录下，MyBatis-Plus自动扫描时与@MapperScan注解的扫描路径重叠
现象：启动日志 "Skipping MapperFactoryBean with name 'pictureMapper'... Bean already defined with the same name!"
解决：generator/目录下的XML仅供代码生成器参考，不影响实际运行，但建议统一管理mapper XML位置

⑤ COS配置未激活导致启动失败
问题：application.yml中cos.client配置被注释，密钥存放在application-local.yml中
现象：启动报错 "Access key cannot be null"（CosClientConfig创建COSClient时secretId为null）
原因：未激活local profile，Spring只加载了application.yml（配置被注释）
解决：在application.yml中添加 spring.profiles.active: local，使application-local.yml中的COS配置生效

⑥ PictureController引用大量未开发模块代码导致编译失败
问题：PictureController是从完整源码直接复制的，包含以下尚未开发模块的引用：
  - Caffeine本地缓存 + Redis分布式缓存（依赖未添加）
  - Sa-Token空间权限体系（SpaceUserAuthManager、StpKit、SaSpaceCheckPermission）
  - 阿里云AI扩图API（AliYunAiApi、CreateOutPaintingTaskResponse）
  - 以图搜图API（ImageSearchApiFacade、ImageSearchResult）
  - 6个未创建的DTO（PictureReviewRequest、PictureUploadByBatchRequest等）
  - Space实体、SpaceService、PictureReviewStatusEnum等
导致61个编译错误（11个包不存在 + 50个找不到符号）
解决：删除所有未开发模块相关的方法和import，仅保留当前已实现的8个核心接口
  - 删除的方法：uploadPictureByUrl、doPictureReview、uploadPictureByBatch、searchPictureByPicture、
    searchPictureByColor、editPictureByBatch、createPictureOutPaintingTask、getPictureOutPaintingTask、
    listPictureVOByPageWithCache（共9个）
  - 替换注解：@SaSpaceCheckPermission → @AuthCheck(mustRole = UserConstant.ADMIN_ROLE)
  - 简化逻辑：listPictureVOByPage用硬编码1替代PictureReviewStatusEnum.PASS.getValue()
     getPictureVOById用已有的checkPictureAuth替代空间权限校验
     updatePicture移除fillReviewParams调用（方法尚未实现）
  共删除约250行代码，移除了16个不存在的import

2026/6/27：图片模块前后端联调与Bug修复

1.前端代码清理 — 移除未开发模块代码
背景：前端代码（AI-TUKU-VIO-FE）是从完整源码项目直接复制的，包含大量尚未开发模块的代码和Vue脚手架样板文件。
删除的文件（14个）：
  - Vue脚手架样板：views/HomeView.vue, views/AboutView.vue（后恢复）, components/HelloWorld.vue, TheWelcome.vue, WelcomeItem.vue,
    components/icons/（5个SVG图标组件）, stores/counter.ts, assets/base.css, assets/main.css, assets/logo.svg
  - 文件测试模块：api/fileController.ts
修改的文件（7个）：
  - router/index.ts：修复"/"路由从废弃的HomeView改为懒加载HomePage
  - api/index.ts：移除fileController的import和export
  - GlobalHeader.vue：删除未开发的"我的空间"下拉菜单项；修复login按钮herf→href拼写错误
  - AddPicturePage.vue：删除不存在的组件引用（UrlPictureUpload, ImageCropper, ImageOutPainting, spaceController）；
    删除URL上传tab和图片编辑工具栏（AI扩图等）；清理空间相关代码（spaceId, fetchSpace, watchEffect）
  - PictureList.vue：删除不存在的ShareModal引用和分享/以图搜图功能
  - PictureDetailPage.vue：删除分享功能；修复@/utils→@/components/utils导入路径
  - PictureManagerPage.vue：修复导入路径（userController→pictureController）；补充审核常量和handleReview桩代码

2.图片模块联调 — 登录后首页报"获取数据失败，系统错误"
问题：使用admin/admin123456登录成功后，首页加载图片列表和标签分类时报"获取数据失败，系统错误"

根因分析：
  - 前一次记录（6/26 第③条）已预警Picture实体字段与数据库表不匹配的问题
  - Picture实体（com.vio.aitukuviobe.model.entity.Picture）定义了thumbnailUrl, picColor, spaceId,
    reviewStatus, reviewMessage, reviewerId, reviewTime共7个字段
  - 但create_table.sql中的picture表没有对应列
  - 首页调用POST /api/picture/list/page/vo → MyBatis-Plus生成SELECT包含全部实体字段
    → MySQL报"Unknown column 'thumbnailUrl'" → RuntimeException → GlobalExceptionHandler
    捕获返回code=50000, message="系统错误"

解决：更新create_table.sql，在picture表中补充7个缺失字段：
  - thumbnailUrl varchar(512) — 缩略图url
  - picColor varchar(16) — 图片主色调
  - spaceId bigint — 空间id
  - reviewStatus int default 0 — 审核状态（0待审核/1通过/2拒绝）
  - reviewMessage varchar(512) — 审核信息
  - reviewerId bigint — 审核人ID
  - reviewTime datetime — 审核时间
  新增idx_reviewStatus索引，提升审核状态查询性能
  同时对已有数据库执行ALTER TABLE补充上述列

3.图片上传大小限制从2MB提升到20MB
问题：管理员上传图片时提示"不能上传超过 2M 的图片"
涉及三处限制：
  - 前端PictureUpload.vue：beforeUpload中硬编码 isLt2M（file.size < 2*1024*1024）
  - 后端FileManager.java：validPicture中硬编码 fileSize > 2 * ONE_M
  - 后端application.yml：spring.servlet.multipart.max-file-size=10MB
解决：三处统一修改为20MB
  - PictureUpload.vue：2→20，变量名isLt2M→isLt20M
  - FileManager.java：2 * ONE_M → 20 * ONE_M
  - application.yml：10MB → 20MB
注意：FileManager.java是实际处理上传的核心校验，只改前端无法生效

4.默认账号密码速查
种子数据来源：sql/test_data.sql
管理员账号：
  - admin / admin123456（角色：admin）
  - admin2 / admin123456（角色：admin）
普通用户账号（密码均为12345678）：
  - wangwu, zhaoliu, sunqi, zhouba, wujiu, zhengshi（角色：user）
密码加密方式：MD5("vio" + 原始密码)，盐值为"vio"
登录接口：POST /api/user/login，请求体{"userAccount":"admin", "userPassword":"admin123456"}

5.图片上传失败 — COS secretId重复导致认证失败
问题：管理员上传图片时提示"图片上传失败，上传失败"（code=50000）

根因分析：
  - 上传流程：PictureUpload.vue → POST /api/picture/upload → PictureController
    → PictureServiceImpl.uploadPicture() → FileManager.uploadPicture()
    → CosManager.putPictureObject() → COSClient.putObject()
  - CosManager调用腾讯云COS时，使用CosClientConfig中的secretId进行认证
  - application-local.yml中secretId的值被意外重复拼接（值本身翻倍，而非正确的原始值）
  - 腾讯云COS返回认证错误（InvalidAccessKeyId）
  - FileManager.uploadPicture()的catch块捕获异常，抛BusinessException(ErrorCode.SYSTEM_ERROR, "上传失败")
  - GlobalExceptionHandler返回{"code": 50000, "message": "上传失败"}
  - 前端PictureUpload.vue显示"图片上传失败，上传失败"

解决：修正application-local.yml第5行secretId值，移除重复部分，恢复为正确的密钥值
涉及文件：src/main/resources/application-local.yml

6.图片上传失败 — COS secretId重复拼接导致认证错误
问题：admin上传图片时前端提示"图片上传失败，上传失败: AccessDenied..."
根因：application-local.yml中secretId的值被意外重复拼接（AKID...TsYAKID...TsY），导致腾讯云COS返回InvalidAccessKeyId认证错误
解决：修正application-local.yml第5行secretId值为正确的单一值
涉及文件：src/main/resources/application-local.yml

7.图片上传失败 — 数据库picture表缺少字段（已预警问题的重现）
问题：修正secretId后上传仍失败，但日志显示"COS上传成功(万象CI)"后又报错
根因：6/26已预警的Picture实体与数据库表不匹配问题 —— COS上传成功，但写入数据库时报Unknown column 'thumbnailUrl'
解决：对已有数据库执行ALTER TABLE补充7个缺失列（thumbnailUrl, picColor, spaceId, reviewStatus, reviewMessage, reviewerId, reviewTime）
涉及文件：sql/create_table.sql（已更新），数据库需手动执行ALTER TABLE

8.图片上传失败 — COS万象CI CAM角色未配置
问题：修复数据库后上传报"Qcloud api role not exist, need create role"（403 AccessDenied）
根因：CosManager.putPictureObject()使用PicOperations调用万象CI图片处理（webp压缩+缩略图），
  需要腾讯云CAM中存在CI_QCSrole服务角色，否则COS拒绝请求
解决：在CosManager.putPictureObject()中添加CosServiceException捕获，
  若错误包含"role not exist"则自动降级为简单putObject（无图片处理），
  后端日志打印warn提示+角色创建链接；同时FileManager做好CI结果为空时的容错处理
涉及文件：src/main/java/com/vio/aitukuviobe/manager/CosManager.java（降级逻辑）
         src/main/java/com/vio/aitukuviobe/manager/FileManager.java（CI结果空值容错）

9.上传成功但图片不显示 — URL双斜杠+缩略图路径丢失
问题：上传成功后图片列表不显示图片，显示默认占位图
根因：
  ① FileManager中URL拼接：host + "/" + uploadPath，uploadPath已以"/"开头，产生双斜杠URL
  ② CosManager中webp/缩略图key使用FileUtil.mainName()只取文件名，丢失目录路径（如public/10001/）
  ③ FileManager中缩略图URL拼接同样丢失目录路径
解决：
  ① 移除多余的"/" → host + uploadPath
  ② CosManager中webp和缩略图key改为基于完整key路径去掉扩展名后拼接
  ③ FileManager中缩略图URL改为基于uploadPath拼接
涉及文件：src/main/java/com/vio/aitukuviobe/manager/FileManager.java（URL拼接修复）
         src/main/java/com/vio/aitukuviobe/manager/CosManager.java（key路径保留）

10.上传成功但图片不显示 — COS桶私有导致403
问题：修复URL后图片仍不显示，浏览器访问图片URL返回403 Forbidden
根因：COS存储桶默认私有，上传对象未设置ACL，浏览器无法直接访问
解决：在CosManager所有上传路径（putObject、putPictureObject主路径、降级路径）中
  添加putObjectRequest.setCannedAcl(CannedAccessControlList.PublicRead)
涉及文件：src/main/java/com/vio/aitukuviobe/manager/CosManager.java（三处添加PublicRead ACL）

11.补充完善 — 上传流程日志+错误信息透传
  - FileManager：添加临时文件创建/写入成功日志；异常捕获时提取e.getMessage()透传给前端
  - CosManager：添加上传开始/成功/失败日志（含bucket、key、size、statusCode、errorCode）
  - 修复FileManager中createTempFile的前缀问题（uploadPath含路径分隔符改为固定前缀"picture_upload_"）
涉及文件：src/main/java/com/vio/aitukuviobe/manager/FileManager.java
         src/main/java/com/vio/aitukuviobe/manager/CosManager.java

2026/6/27：用户传图模块后端开发

本次开发遵循 `开发步骤/5-用户传图模块开发步骤.md`，包含三个子模块：用户上传审核、URL上传、批量抓取。

═══════════════════════════════════════════════════════════
模块一：用户上传图片及审核
═══════════════════════════════════════════════════════════

1. 数据模型开发

① 新建审核状态枚举 PictureReviewStatusEnum
文件：model/enums/PictureReviewStatusEnum.java
三个枚举值：REVIEWING(待审核, 0) / PASS(通过, 1) / REJECT(拒绝, 2)
提供 getEnumByValue(Integer value) 静态方法根据value获取枚举
用于替代代码中的魔法值 0/1/2，提升可读性和可维护性

② Picture实体已包含审核字段（上次开发已添加）
reviewStatus, reviewMessage, reviewerId, reviewTime 四个字段已在Picture实体中定义
数据库需执行ALTER TABLE添加对应列并创建索引（若尚未执行）

③ PictureQueryRequest已包含审核查询字段（上次开发已添加）
支持按 reviewStatus/reviewMessage/reviewerId 筛选

2. 新建请求包装类 PictureReviewRequest

文件：model/dto/picture/PictureReviewRequest.java
包含字段：id（图片ID）、reviewStatus（审核状态）、reviewMessage（审核信息）
用于管理员审核操作的请求体

3. 开发审核服务方法 doPictureReview

接口定义：PictureService.java 新增方法签名
实现逻辑（PictureServiceImpl.doPictureReview）：
  ① 校验参数：id非空、reviewStatus非空且不能为REVIEWING(0)
  ② 判断图片是否存在
  ③ 检查是否已是该状态（防止重复审核，如已通过再点通过）
  ④ 更新审核状态，设置 reviewerId + reviewTime
  ⑤ updateById 持久化到数据库

4. 开发审核接口 POST /picture/review

权限：仅管理员可用（@AuthCheck(mustRole = UserConstant.ADMIN_ROLE)）
Controller层接收 PictureReviewRequest，调用 doPictureReview 完成审核
返回 BaseResponse<Boolean>

5. 提取通用方法 fillReviewParams

在 PictureService 接口中定义，PictureServiceImpl 中实现：
  管理员登录 → 自动过审：reviewStatus=PASS(1)，设置reviewerId、reviewMessage="管理员自动过审"、reviewTime
  普通用户登录 → 设为待审核：reviewStatus=REVIEWING(0)

该方法从 uploadPicture 和 editPicture 中提取出的重复逻辑，统一管理审核参数设置

6. 各接口集成 fillReviewParams

| 位置 | 集成方式 |
|------|----------|
| uploadPicture 服务方法 | 构造Picture对象后、入库前调用 |
| editPicture 服务方法 | 更新前调用 fillReviewParams |
| updatePicture Controller | 管理员更新时调用 fillReviewParams |

7. 权限控制细化

① POST /picture/upload 移除 @AuthCheck(mustRole = UserConstant.ADMIN_ROLE)
  → 允许普通用户上传图片
  → 服务层 uploadPicture 中添加细粒度权限：更新时仅本人或管理员可编辑

② POST /picture/edit 移除 @AuthCheck(mustRole = UserConstant.ADMIN_ROLE)
  → 允许普通用户编辑自己的图片
  → 服务层 editPicture 中添加细粒度权限：仅本人或管理员可编辑

设计思路：从接口级别的"一刀切"管理员权限，改为方法内的"仅本人或管理员"细粒度控制

8. 内容可见性控制（已在上次开发完成）

listPictureVOByPage：普通用户默认只查看 reviewStatus=1（审核通过）的数据
getQueryWrapper：已支持 reviewStatus/reviewMessage/reviewerId 查询条件
管理员可通过 listPictureByPage 查看所有审核状态的图片

═══════════════════════════════════════════════════════════
模块二：通过 URL 导入图片（模板方法模式重构）
═══════════════════════════════════════════════════════════

1. 模板方法模式重构上传逻辑

新建 manager/upload 包，统一管理模板方法相关代码

① 抽象类 PictureUploadTemplate
文件：manager/upload/PictureUploadTemplate.java
定义上传流程模板方法 uploadPicture(inputSource, uploadPathPrefix)：
  1. validPicture(inputSource) — 校验（抽象方法，由子类实现）
  2. 生成唯一上传路径：public/{userId}/日期_UUID.后缀
  3. 创建临时文件 → processFile(inputSource, file) — 处理文件（抽象方法）
  4. 上传到COS（cosManager.putPictureObject）
  5. buildResult() — 封装结果（含CI图片信息提取、缩略图URL生成）
  6. deleteTempFile() — finally中清理临时文件

容错处理：
  - CI万象结果为空时降级，使用文件扩展名作为格式
  - URL双斜杠修复：host + uploadPath（uploadPath已以"/"开头）
  - 缩略图URL基于uploadPath拼接，保留完整目录路径

② 子类 FilePictureUpload（@Service）
文件：manager/upload/FilePictureUpload.java
  validPicture：校验文件大小 ≤ 2MB，后缀白名单 jpeg/jpg/png/webp
  getOriginFilename：返回 MultipartFile.getOriginalFilename()
  processFile：multipartFile.transferTo(file) 写入临时文件

③ 子类 UrlPictureUpload（@Service）
文件：manager/upload/UrlPictureUpload.java
  validPicture（五步校验）：
    1. new URL(fileUrl) 验证URL格式
    2. 校验协议（仅http/https）
    3. HEAD请求验证文件存在（失败不阻塞，仅警告）
    4. 校验Content-Type（image/jpeg, image/jpg, image/png, image/webp）
    5. 校验Content-Length（≤2MB）
  getOriginFilename：从URL提取主文件名
  processFile：HttpUtil.downloadFile(fileUrl, file) 下载到临时文件

设计要点：
  - HEAD请求失败不抛异常，仅对能获取到的信息校验，提高URL导入成功率
  - 子类通过 @Service 注解注册为Spring Bean，父类中 @Resource 注入的 CosManager/CosClientConfig 由Spring管理

2. PictureUploadRequest 扩展

新增字段：
  fileUrl（String）— URL上传时使用
  picName（String）— 自定义图片名称（批量上传时使用序号前缀）

3. uploadPicture 服务方法重构

签名变更：
  旧：PictureVO uploadPicture(MultipartFile multipartFile, PictureUploadRequest, User)
  新：PictureVO uploadPicture(Object inputSource, PictureUploadRequest, User)

路由逻辑：根据 inputSource 类型自动选择上传模板
  inputSource instanceof String → urlPictureUpload
  其他（MultipartFile） → filePictureUpload

图片名称优先级：picName > name > 原始文件名

4. URL上传接口 POST /picture/upload/url

权限：登录即可（无管理员限制）
接收JSON请求体中的 fileUrl，调用 uploadPicture(fileUrl, ...)

5. FileManager 标记废弃

原有的 FileManager 已有 @Deprecated 注解（上次开发时添加）
新上传逻辑统一通过模板方法模式处理

═══════════════════════════════════════════════════════════
模块三：批量抓取和创建图片
═══════════════════════════════════════════════════════════

1. 引入 jsoup 依赖

pom.xml 新增：
  <dependency>
      <groupId>org.jsoup</groupId>
      <artifactId>jsoup</artifactId>
      <version>1.15.3</version>
  </dependency>
jsoup 用于解析HTML页面，从必应图片搜索结果中提取图片URL

2. 新建请求体 PictureUploadByBatchRequest

文件：model/dto/picture/PictureUploadByBatchRequest.java
字段：
  searchText（String）— 搜索关键词
  count（Integer，默认10）— 抓取数量，上限30
  namePrefix（String）— 图片名称前缀，为空时使用搜索词

3. 批量抓取服务 uploadPictureByBatch

接口定义：PictureService.java 新增
实现逻辑（PictureServiceImpl.uploadPictureByBatch）：
  ① 校验抓取数量 ≤ 30
  ② 拼接必应图片搜索URL：https://cn.bing.com/images/async?q={searchText}&mmasync=1
  ③ Jsoup.connect(fetchUrl).get() 获取HTML
  ④ 定位 div.dgControl → 提取 img.mimg 元素列表
  ⑤ 遍历元素：
     - 获取 src 属性
     - 去除URL中"?"后的参数（防止转义问题）
     - 调用 this.uploadPicture(fileUrl, pictureUploadRequest, loginUser) 逐张上传
     - 设置图片名称：namePrefix + 递增序号
     - 单张失败catch后continue，不中断流程
     - 达到目标数量时break退出

容错设计：
  - 空链接跳过（log.info记录）
  - 单张上传失败不中断（log.error记录后continue）
  - 名称前缀为空时默认使用搜索词

4. 批量上传接口 POST /picture/upload/batch

权限：仅管理员可用（@AuthCheck(mustRole = UserConstant.ADMIN_ROLE)）
接收 PictureUploadByBatchRequest，返回成功创建的图片数量 Integer

═══════════════════════════════════════════════════════════
文件变更汇总
═══════════════════════════════════════════════════════════

新建文件（6个）：
  model/enums/PictureReviewStatusEnum.java     — 审核状态枚举
  model/dto/picture/PictureReviewRequest.java   — 审核请求DTO
  model/dto/picture/PictureUploadByBatchRequest.java — 批量上传请求DTO
  manager/upload/PictureUploadTemplate.java     — 上传模板抽象类
  manager/upload/FilePictureUpload.java         — 本地文件上传实现
  manager/upload/UrlPictureUpload.java          — URL上传实现

修改文件（5个）：
  service/PictureService.java                   — 新增3个方法签名，修改uploadPicture签名
  service/impl/PictureServiceImpl.java           — 实现所有新方法，重构上传逻辑
  controller/PictureController.java             — 新增3个端点，移除2个@AuthCheck
  model/dto/picture/PictureUploadRequest.java   — 新增fileUrl、picName字段
  pom.xml                                       — 新增jsoup依赖

═══════════════════════════════════════════════════════════
关键技术点
═══════════════════════════════════════════════════════════

• 审核状态枚举 PictureReviewStatusEnum：统一管理审核状态值，替代魔法值0/1/2
• 模板方法模式：父类定义上传骨架，子类实现差异化步骤（本地文件 vs URL下载）
• 细粒度权限：从接口级 @AuthCheck 改为方法内"仅本人或管理员"判断
• 内容可见性：普通用户列表默认过滤 reviewStatus=1
• jsoup爬虫：解析必应图片搜索结果，批量导入
• 容错处理：批量导入单张失败不中断；URL HEAD校验失败不阻塞
• CI降级：万象CI不可用时自动降级为基础上传，使用文件扩展名作为格式

2026/6/27：用户传图模块 — Bug修复记录

1. 编译错误：程序包 org.jsoup 不存在

问题：IDE 编译时报"程序包org.jsoup不存在"、"程序包org.jsoup.nodes不存在"、"程序包org.jsoup.select不存在"
根因：pom.xml 中 jsoup 依赖已正确添加，但 Maven 无法连接到中央仓库下载依赖
详细链：
  - Maven 尝试从 https://repo.maven.apache.org/maven2 下载 org.jsoup:jsoup:1.15.3
  - DNS 解析失败："不知道这样的主机 (repo.maven.apache.org)"
  - 下载失败被缓存到本地仓库（~/.m2/repository/org/jsoup/），后续不再重试
  - 同时导致所有其他依赖（spring-boot、mybatis-plus、cos_api、hutool 等）也无法下载
  - 本质原因是国内网络环境无法直接访问 Maven 官方中央仓库

解决：
  ① 创建 ~/.m2/settings.xml，配置阿里云 Maven 镜像：
     <mirrors>
         <mirror>
             <id>aliyunmaven</id>
             <mirrorOf>central</mirrorOf>
             <name>阿里云公共仓库</name>
             <url>https://maven.aliyun.com/repository/public</url>
         </mirror>
     </mirrors>
  ② 删除本地缓存的失败下载记录：rm -rf ~/.m2/repository/org/jsoup
  ③ 在 IDEA 中右键 pom.xml → Maven → Reload Project 重新加载依赖

涉及文件：~/.m2/settings.xml（新建）、pom.xml（jsoup 依赖配置无误）
